# Astvard — записки для агента

Прочитай это до того, как что-то менять. Здесь то, чего не видно в коде:
почему сделано именно так и на чём тут уже обжигались.

## Что в репозитории

Две почти не связанные вещи под одной крышей:

| Часть | Что это | На чём |
|---|---|---|
| `server.js` `src/` `client/` `public/` `db.js` | портал astvard.online: публичный сайт, кабинет, админка, вайтлист, статус серверов | Node.js без фреймворков, Postgres |
| `valheim-mod/` | мод Valheim: админ-панель в игре, шаблоны построек, рельеф, автоматизация | C# net472, BepInEx + Jötunn |

**Портал — это админ-панель хозяина** (`vanilla-admin-panel`), перенесённая сюда
12.09.2026 целиком: свой сервер на `node:http`, схема в `db/schema.sql`,
сессии в базе, роли `Superadmin/Admin/User`, статьи, медиатека, уведомления, логи,
2FA. Прежний портал (`backend/`, `frontend/` и прежний `db/`) снесён; он остался
в истории git, и оттуда же перенесены наши три вещи — вход через Steam, вайтлист и
статус серверов. Модули панели про электрику (сотрудники, инструмент, автопарк,
заявления, учёт времени) убраны из меню, но их код и таблицы пока на месте.

Общего кода у них нет — собираются, запускаются и ломаются порознь.
`deploy/` — установка на VPS: сайт, nginx, Valheim в Docker; как и почему — `deploy/README.md`.
`placeholder/` — заглушка сайта и её конфиг nginx; на VPS с 11.09.2026 её сменил сайт.
`valheim-mod/decompiled_*.cs` — куски игры для справки, в git не лежат.

## Как поднять

```bash
docker compose up -d   # Postgres на 127.0.0.1:5432, встаёт и после перезагрузки
npm install            # ничего нативного: pg — обычный JavaScript
npm start              # сайт на :3001, читает .env из корня
```

База — Postgres из `docker-compose.yml`: роль и база `astvard`, пароль `astvard_dev`
или `POSTGRES_PASSWORD` из `.env`. Схему панель накатывает сама при старте из
`db/schema.sql` и сама заводит настройки и демо-аккаунты на чистой базе. Тестам нужна
своя база рядом, заводится один раз:

```bash
docker exec astvard-postgres createdb -U astvard astvard_test
npm test
```

Агенту с браузерной панелью удобнее поднимать сайт через `preview_start`
по имени `astvard-backend` — запись уже лежит в `.claude/launch.json`,
тогда логи читаются штатно, а не из перехваченного вывода.

Игровой сервер — только через свой bat, из его же папки. Процесс надо **отцепить**:
команда агента завершается вместе с вызовом и утащила бы сервер за собой. И сразу
`-WindowStyle Hidden` — иначе на рабочем столе хозяина машины открывается окно cmd,
которое там никому не нужно: весь вывод и так пишется в `-logFile`.

**Сервер, запущенный из Claude, живёт, пока живёт приложение Claude.** Claude Desktop —
пакет MSIX, и всё, что его инструменты запускают, даже через `Start-Process`, остаётся в
его контейнере. 11.09.2026 в 02:08 приложение само обновилось, Windows закрыла процессы
пакета, и сервер умер без `OnApplicationQuit`: лог оборвался на плановой строке в 02:01,
в журнале Windows рядом — «cmd.exe was closed» и установка пакета Claude. Мир уцелел
только потому, что никто не играл: последнее сохранение было за семь минут до этого.
То же будет при закрытии приложения.

**И так и задумано: этот сервер — отладочный.** Хозяин решил 11.09.2026: ни планировщика,
ни автозапуска не нужно — агент поднимает и останавливает сервер сам, а настоящий сервер
будет на VPS. Значит, в начале работы с модом проверь, жив ли `valheim_server`, и если
нет — подними его, как написано ниже; упавший вместе с Claude сервер — не авария.

**Запускать из PowerShell, не из Bash.** Сервер, поднятый из Bash через
`powershell -c "Start-Process …"`, потом глух к Ctrl+C: `send_ctrl_c.ps1` отрабатывает с
кодом 0, а игра не выходит. Похоже, запрет Ctrl+C наследуется от оболочки Bash через всю
цепочку до `valheim_server.exe`. Проверено 10.09.2026 на двух экземплярах подряд:
поднятый из Bash простоял минуту после сигнала и был убит; поднятый той же командой из
PowerShell вышел за 4.4 с и записал сохранение № 19.

```powershell
Start-Process 'C:\Games\steamapps\common\Valheim dedicated server\start_astvard.bat' -WorkingDirectory 'C:\Games\steamapps\common\Valheim dedicated server' -WindowStyle Hidden
```

**Останавливать — только Ctrl+C в его консоль.** Тогда игра пишет мир на выходе: в логе
`Game - OnApplicationQuit`, `ZNet Shutdown`, `World save (5/5) done`, а в
`worlds_local\AstwardWorld` появляется следующий номер `_main.N.*`. Убитый процесс теряет
всё после последнего автосохранения (`-saveinterval 900`, то есть до 15 минут). Окно
скрыто, поэтому Ctrl+C нажимает `valheim-mod/server/send_ctrl_c.ps1`, и только отдельным
скрытым процессом: ему приходится бросить свою консоль и подцепиться к консоли сервера.
`cmd.exe` от bat после этого остаётся висеть в скрытом окне (судя по всему, на «Terminate
batch job (Y/N)?») — его добить, но **только после того, как сервер вышел**: убитый
раньше `cmd.exe` сервер не остановит. Если за минуту сервер не вышел, он глух к Ctrl+C
(см. выше про Bash) — тогда сверить время последнего `World save (5/5) done` с последним
действием игроков и только потом убивать. Проверено 10.09.2026: сервер вышел за 2.6 с и
записал сохранение № 17. Из корня репозитория:

```powershell
$server = (Get-Process valheim_server).Id
Start-Process powershell -ArgumentList '-NoProfile','-ExecutionPolicy','Bypass','-File',"$PWD\valheim-mod\server\send_ctrl_c.ps1",'-TargetPid',$server -WindowStyle Hidden -Wait
# дождаться, пока valheim_server исчезнет, и проверить в логе World save (5/5) done, затем:
Get-CimInstance Win32_Process -Filter "Name='cmd.exe'" | Where-Object CommandLine -match 'start_astvard' | ForEach-Object { Stop-Process -Id $_.ProcessId -Force }
```

Проверка, что всё действительно живо (не верь тому, что процесс просто есть):

```bash
curl -s http://127.0.0.1:3001/api/health          # ждём {"status":"ok","db":"ok"}
netstat -ano -p UDP | grep :2456                  # игровой сервер слушает
tail -5 "/c/Games/valheim-servers/astvard/logs/astvard_server.log"
```

В логе ждём `Valheim version: ...` и `Game server connected`. Смотреть надо именно
в этот файл, а не в `Player.log`: для Unity клиент и сервер оба «IronGate/Valheim»,
пишут по одному пути и отбирают его друг у друга — ошибки клиента там легко принять
за серверные. Отсюда и `-logFile` в bat.

**Логов два, и мода в этом нет.** `-logFile` перехватывает только вывод Unity, включая
прелоадер BepInEx. Сам загрузчик после старта пишет к себе, в
`BepInEx\LogOutput.log` внутри папки установки. Строки `Loading [Jotunn]` и
`Loading [AstvardServerMod]` искать надо там, и «мода нет в логе сервера» почти всегда
означает, что смотрели не в тот файл. Второй сервер на этой же установке будет писать
в тот же `LogOutput.log` — когда дойдёт до этого, разводить через `BepInEx.cfg`.

**`Connections N` в логе сервера — не всегда игроки.** Генератор подземелий пишет
свою строку `Connections N` — про проходы между комнатами, рядом с `Available rooms`
и `placed N doors`. Игроков считает только строка вида `Connections N ZDOS:… sent:… recv:…`,
а кто именно зашёл — `Got connection SteamID <число>`.

## Сервер: как он собран и почему именно так

Пересобран 10.09.2026 с нуля. Каждый флаг в `start_astvard.bat` выбран по коду
`assembly_valheim.dll` 1.0.7, а не по вики, и **отсутствие флага — тоже решение**.
Причины расписаны в шапке самого bat; здесь то, что важно помнить снаружи.

- **Сохранения переехали из папки игры** в `C:\Games\valheim-servers\astvard`.
  Обновление или проверка целостности Steam туда не дотянется, и вторая установка
  потом сможет делить этот же бинарник со своим `-savedir`.
- **Вход по вайтлисту, пароля нет.** Пароль игра требует только у публичного сервера,
  а этот `-public 0`. `permittedlist.txt` пустой означает «пускать всех»; список
  включается с первой же строки. Оба списка перечитываются на ходу, примерно раз в
  десять секунд, и выпавшего из вайтлиста кикает сразу. Но **только если файл новее
  прочитанного**: `SyncedList.Load` (в `assembly_utils.dll`) сравнивает время изменения
  файла с последней загрузкой и выходит, если оно не больше. Список, положенный со
  старой датой (`scp -p`, распаковка архива), игра молча не заметит до перезапуска;
  после такого копирования — `touch`. Ошибки чтения там же глотаются пустым `catch`.
  Концы строк неважны: читает `StreamReader.ReadLine()`, ему годятся и `\r\n`, и `\n`.
- **`-public 0` прячет сервер и от A2S.** `ZSteamMatchmaking.RegisterServer` зовёт
  `SteamGameServer.SetAdvertiseServerActive(true)` только для публичного сервера, а без
  этого Steam не отвечает на запросы серверного браузера. Проверено 11.09.2026 на VPS:
  на `A2S_INFO` молчат и 2456, и 2457 — снаружи, с самой машины и по адресу контейнера,
  хотя игра оба порта держит. Поэтому опрос статуса сайта (`backend/protocols/a2s.js`)
  этот сервер не увидит никогда. С 12.09.2026 статус берётся из **его собственного
  лога**: у строки сервера в базе `probe = 'valheim-log'`, разбор — в
  `backend/protocols/valheim-log.js`, путь к файлу — `VALHEIM_LOG_FILE`. Игра пишет
  `Connections N ZDOS:` раз в десять минут, и раньше этим счётчиком всё и
  ограничивалось; с 12.09.2026 состав онлайна собирается из строк входа и выхода
  (`Got connection SteamID`, `Closing socket`), то есть точность мгновенная — см.
  раздел «Незакрытое». «Онлайн» решается свежестью файла по `mtime` (не по времени
  внутри строк — оно в часовом поясе контейнера) и тем, что после последнего
  сердцебиения нет `OnApplicationQuit`/`ZNet Shutdown`.
- **ID пишется как `V_<SteamID64>`, и только так.** `ZNet.ListContainsId` прогоняет
  пришедший с сокета ID через `PlatformUserID.FilterPlatformUserID`, где платформа
  `Steam` превращается в префикс `V`, а последняя ветка **присваивает** флаг совпадения,
  а не добавляет его — так что голая цифра и `Steam_…`, проверенные до неё, выбрасываются.
  В `adminlist.txt` при этом стоит и `Steam_…`: `ZNet.PlayerIsAdmin` сравнивает сырым,
  и от него зависит кнопка кика в игровом списке игроков.
- **`-crossplay` не включён намеренно.** Он меняет весь транспорт на PlayFab Party:
  слушающего сокета нет, вход по IP превращается в поиск по лобби и отказывается от
  приватных адресов, серверная информация для A2S не публикуется вовсе, а личность пира
  приходит полем, которое написал сам клиент, вместо тикета, проверенного через
  `BeginAuthSession`. Для Steam-компании на белом IP это всё минусы без единого плюса.
- **`-preset` не передаётся.** Пресет вписывается в мир **при каждом старте** и остаётся
  там до `-resetmodifiers`. Старый мир до сих пор носит `nobuildcost`, `passivemobs` и
  `eventrate 0` от одного запуска с `-preset Hammer`.
- **Сид командной строкой не задаётся, флага нет.** Мир, созданный сервером, получает
  десять случайных символов из `World.GenerateSeed()`. Чтобы выбрать свой — создать мир
  клиентом и положить папку в `worlds_local`. Личность мира — **имя папки**:
  `SaveSystem.GetChunkedSaveName` это буквально
  `Path.GetFileName(Path.GetDirectoryName(path))`. Файлы внутри не переименовывать.
- **Кэш биомов привязан к имени мира** (`cache\<Мир>_biomedatacache.bin`). Подкладывая
  чужую карту под то же имя, кэш надо удалить, иначе он будет принят как свой.

Старый мир не удалён, лежит в
`C:\Games\steamapps\common\Valheim dedicated server\saves.removed-20260910-0052`.
Прежний bat — рядом, как `start_astvard.bat.old`.

**Клиент вычищен до ванили** был 09.09.2026 и с тех пор BepInEx на нём поставлен заново.
Содержимое старого `BepInEx/config` лежит в `.backups/valheim-client-bepinex-config/` —
33 файла, сверено хешами. Там `astvard-templates` с чертежами построек: имена файлов
врут, судить надо по заголовку `#name` внутри (`y.txt` — это база на 751 деталь).

`.env` не в git. Ключи портала: `PORT=3001`; `APP_URL` — абсолютный адрес сайта, на
него Steam возвращает игрока, и этот адрес входит в подпись ответа (из заголовка
`Host` его брать нельзя, его пишет тот, кто стучится): локально
`http://localhost:3001`, на VPS `https://astvard.online`; `POSTGRES_HOST`,
`POSTGRES_PORT`, `POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB` — база;
`TRUST_PROXY=true` на VPS — nginx терминирует HTTPS, без этого кука сессии не получит
флаг `Secure`; `VALHEIM_LOG_FILE` — лог игрового сервера для строк с
`probe = 'valheim-log'`; `VALHEIM_MOD_CONFIG` — папка конфигов мода
(`/srv/valheim/server/BepInEx/config`), откуда раздел «Наш сервер» берёт руны и
общие постройки; `VALHEIM_SAVES_DIR` — папка сохранений игрового сервера
(`/srv/valheim/saves`), **единственное место, куда сайт пишет** за своими
пределами: два файла списков доступа; `GAME_LISTS_TOKEN` — токен, по которому мод
игрового сервера забирает списки с `/api/game/lists` (тот же вписывается моду в
`[Сайт] ListsToken`, без токена маршрута нет вовсе); `SUPERADMIN_STEAM_ID` — SteamID64 хозяина: `src/bootstrap.js`
при каждом старте смотрит, есть ли хоть один `Superadmin`, и если нет — заводит или
повышает этот аккаунт, сразу с одобренным вайтлистом и админкой в игре. Пока этот ключ
задан, `db.js` **не создаёт** демо-аккаунты `superadmin/admin/user` с паролем
`1234qwer`: на живом сайте это не вход, а дыра с табличкой. Пароля у хозяина нет
вовсе — вход через Steam.

На VPS есть ещё `POSTGRES_CONTAINER=postgres-shared` — в каком чужом контейнере живёт
наша база; его читают только скрипты в `deploy/`, самой панели он безразличен.

**JWT в панели нет вовсе.** Сессия — случайный токен в куке `session`, строка в таблице
`sessions` и запись в Map. `JWT_SECRET`, если он остался в чьём-то `.env`, — от прежнего
портала, и никто его не читает.

**Миграций нет: `db/schema.sql` накатывается поверх живой базы при каждом старте** и
обязан оставаться идемпотентным. `CREATE TABLE IF NOT EXISTS` пропускает существующую
таблицу целиком, вместе с колонками, поэтому каждая колонка, добавленная после первого
развёртывания, есть в файле **дважды** — в `CREATE TABLE` для чистой базы и отдельным
`ALTER TABLE ... ADD COLUMN IF NOT EXISTS` в хвосте файла. Добавляешь колонку —
добавляй в оба места, иначе на боевой базе её не будет.

Так сделано после того, как миграции панели один раз уже подвели: версия отмечается
выполненной в тот же миг, когда миграция выдана, и падение внутри неё оставило бы работу
несделанной навсегда. Из-за этого в базе не оказалось `notifications.scheduled_at` —
нашлось только при переносе схемы.


## VPS: astvard.online и настоящий сервер

Подняты 11.09.2026. Вход с этой машины — `ssh astvard-vps` (root). **Машина общая**:
чужие PM2-приложения, сайты в том же nginx, n8n и общий Postgres `postgres-shared` —
их не трогать. Перезагрузка висит в очереди после обновлений системы и делается только
с согласия хозяина. Как всё ставилось и почему — `deploy/README.md`.

- **Сайт** — `/srv/astvard`, клон ветки `valheim-mod`; служба `astvard-backend`
  запускает `node /srv/astvard/server.js`, nginx отдаёт всё на `:3001`. **База — своя
  роль и своя база `astvard_panel` внутри чужого контейнера `postgres-shared`**:
  подключение к ней закрыто для `PUBLIC`, пароль — в `/srv/astvard/.env` и больше
  нигде. Загрузки — `/srv/astvard/uploads`; они в `.gitignore` и переживают обновление,
  но не `git clean -xdf`, поэтому его здесь не запускать. Обновить — `git push`, потом
  на VPS `/srv/astvard/deploy/update-site.sh`: он сам откатится, если `/api/health` не
  ответит. Поменялся юнит systemd — запускать `install-site.sh`, обновление его не
  трогает. База `astvard` в том же контейнере осталась от прежнего портала — не нужна,
  но и не удалена.
- **Игровой сервер** — Docker-контейнер `astvard-valheim`, всё в `/srv/valheim`: игра в
  `server/`, мир и списки в `saves/`, лог игры `logs/server.log`, лог BepInEx и мода —
  `server/BepInEx/LogOutput.log`, настройки — `server.env`. Команды — из `/srv/astvard`
  через `docker compose -f deploy/valheim/compose.yml`.
- **Мир на VPS — копия `AstwardWorld`** с Windows-сервера, сохранение № 72 от 11.09.2026
  22:02. С этого момента миры разошлись. Туда же перенесены списки доступа, настройки
  мода и четыре общих шаблона; руны — нет, счёт на VPS начался с нуля.
- **Останавливать — только `stop`** (или `down`, `restart`): в compose стоит
  `stop_signal: SIGINT`. Проверено 11.09.2026: остановка за 3.9 с, код выхода 0,
  `World save (5/5) done`, сохранение № 73. `docker kill` и `docker rm -f` теряют всё
  после последнего автосохранения.
- **Мод на Linux работает** (`BEPINEX=yes` в `server.env`): BepInEx 5.4.23.3 через
  doorstop, `Loading [Jotunn 2.30.0]`, `Loading [AstvardServerMod 1.0.0]`,
  `Kept zones: ZoneSystem.PokeLocalZone bound.`, ни одной ошибки или предупреждения
  (12.09.2026). **Хозяин на него зашёл 12.09.2026**: вайтлист пропустил, мод выдал
  админку, панель и шаблоны работают — подробности ниже, в незакрытом.
- **`DllNotFoundException: libParty.so` в логе игры — не наша и не мешает.** Файл
  лежит как `valheim_server_Data/Plugins/libparty.so`, а игра просит `libParty.so`; на
  Linux регистр важен, и PlayFab Party не поднимается. Он нужен только `-crossplay`,
  которого у нас нет.
- **Обновление игры поверх разложенного BepInEx проверено — BepInEx цел** (18.09.2026,
  VPS с 1.0.12 на 1.0.15). `app_update 896660 validate` не тронул ни одного чужого файла:
  41 файл в `BepInEx/` и `doorstop_libs/` до и после, `libdoorstop_x64.so` на месте, DLL мода
  та же по хешу. После обновления `Loading [Jotunn 2.30.0]`, `Loading [AstvardServerMod
  1.1.0]`, ошибок нет.
- **Но SteamCMD может отказаться обновлять, не начав качать.** Трижды подряд:
  `Update state (0x3) reconfiguring` → `(0x0) unknown` → `Error! App '896660' state is 0x6
  after update job`, ноль байт скачано. Ни место (180 ГБ свободно), ни память (13 ГБ) ни при
  чём, и без `validate` то же самое. Лечится сносом `steamapps/appmanifest_896660.acf`:
  следующий заход прошёл за полторы минуты и сказал `Success! App '896660' fully installed`.
  Манифест перед сносом стоит отложить — копия легла в `/root/appmanifest_896660.acf.bak`.
- **Сетевая версия 1.0.15 осталась 40**, как у 1.0.12 и 1.0.14, так что клиент постарше
  подключается по-прежнему. Игру обновляет **отдельная команда**
  (`docker compose … run --rm valheim update`), а не перезапуск контейнера: от `restart` или
  `stop`/`up` игра не обновится сама, и сюрприза не будет.
- **Осталось непроверенным:** не тронет ли SteamCMD
  `validate` чужие файлы, не проверено: после первого такого обновления смотреть, на
  месте ли `BepInEx/` и `doorstop_libs/`, и строки `Loading [...]` в логе BepInEx.

## Вход через Steam и вайтлист

**Регистрации как формы в портале нет** (с 12.09.2026). Аккаунт заводится сам при
первом входе через Steam: на игровой сервер пускают по номеру, а не по почте, и
аккаунт без Steam не может ни попросить доступ, ни получить его. Прежняя форма
(почта, пароль, ФИО, тип аккаунта, honeypot и арифметическая капча) осталась от
учётной системы компании и заводила ровно такие пустые аккаунты — снесена вместе с
`/api/auth/register` и `client/register.html`; адрес страницы жив и ведёт на вход.
Переключатель «Принимать новых игроков» (`allow_registration`) теперь означает
именно это: выключен — незнакомый номер Steam аккаунта не заводит, уже заведённые
входят.

Пароль при этом остался: он есть у аккаунтов, которые админ заводит в панели, и его
может задать себе любой игрок — в кабинете, в блоке «Дополнить аккаунт». Там же
меняются ник и почта. Правила ника живут в `src/nickname.js` и знают про «ё»;
текущий пароль спрашивается только у того, у кого он уже есть — иначе первый пароль
задать было бы нечем. Почта приводится к нижнему регистру при записи: иначе
`Ivan@mail.ru` и `ivan@mail.ru` — два аккаунта на один живой адрес.


В вайтлист ведут две дороги. **Обычная:** игрок жмёт «Войти через Steam»,
возвращается с подтверждённым `SteamID64`, отдельной кнопкой просит доступ — может
приложить пару слов о себе, — админка показывает заявку, админ решает. **Короткая:**
админ сам вписывает номер в админке, для тех, кто на сайт не заходил; запись сразу
одобрена, заявки не было. **Решение доезжает до сервера само** (с 12.09.2026): после каждой выдачи, отказа и
переключения админки сайт переписывает `permittedlist.txt` и `adminlist.txt` прямо
в папке сохранений, а игра перечитывает их секунд за десять. Запись — во временный
файл рядом и переименование: так получается и атомарность, и свежий mtime, без
которого `SyncedList.Load` файл игнорирует. Прежний файл остаётся рядом как `.bak`.
Кнопка «Применить на сервере» в админке делает то же самое руками — на случай,
когда файлы разъехались с базой. Кнопки «Собрать …» никуда не делись: они нужны,
когда сервер живёт не на этой машине. **Пустой `permittedlist.txt` не пишется
никогда** — для игры он означает «пускать всех».

**Приём заявок бывает автоматический**: переключатель «Принимать заявки на сервер
сразу» в настройках. Включён — заявка игрока сама становится одобренной и тут же
уезжает в файл, то есть человек может заходить через десяток секунд. Выключен —
как раньше, заявка ждёт админа.

Рядом, из той же таблицы, выдаётся **админка на игровом сервере** —
`adminlist.txt`. Это не то же самое, что роль на сайте: `role` — права в портале,
`server_admin` — права в игре, и совпадать они не обязаны.

В админке с 12.09.2026 две таблицы вместо одной: «Заявки» — то, на что надо ответить
сегодня, «Доступ на сервер» — **все, кто привязал Steam**, с их состоянием. Вторая
таблица держит и тех, кто ничего не просил: выдать доступ такому человеку — обычное
дело, а раньше до него можно было добраться только через поле «добавить по SteamID64».
Поэтому «Выдать доступ» идёт через `grantWhitelist` (ему нужен только `steam_id`), а
«Отклонить» — через `decideWhitelist`, который по-прежнему отказывается решать за того,
кто не спрашивал. **«Забрать доступ» возвращает строку в `none` и заодно снимает
`server_admin`**: права в игре и право войти — разные вещи, но админ, которому вход
закрыт, это право, о котором забудут, а пустой `permittedlist.txt` пускает всех —
именно в этот момент забытая строка в `adminlist.txt` и сработает.

**Steam говорит на OpenID 2.0, и ключ для этого не нужен.** Два перехода:
`/api/auth/steam` уводит к Steam, `/api/auth/steam/return` принимает ответ.

- **Ответ Steam — не доказательство, пока Steam его не подтвердил.** Всё приходит
  в адресной строке игрока, то есть это текст, который может написать кто угодно.
  Единственное, что делает его доказательством, — обратный запрос к Steam с
  `openid.mode=check_authentication` и строка `is_valid:true` в ответе. **Без этого
  шага любой желающий входит любым SteamID, введя URL руками.** Проверено 11.09.2026
  живьём: подделанный ответ с чужим номером Steam отверг, куку сайт не выдал.
- **Подпись покрывает только поля из `openid.signed`.** Ответ, где `claimed_id` вне
  этого списка, несёт номер, который выбрал отправитель, — а `check_authentication`
  на остальное всё равно ответит «годно». Поэтому список проверяется отдельно.
- **`return_to` сверяется с нашим адресом.** Он входит в подпись, и это то, что не
  даёт переиграть на нашем сайте ответ, выписанный для чужого.
- **Steam недоступен — значит «не вошёл».** Любая ошибка сети по пути к
  `check_authentication` обязана читаться как отказ, никогда как успех.
- **Ручной ввод номера убран совсем.** Пока он существовал, игрок мог вписать чужой
  `SteamID64`: занять его (колонка уникальна) и подвести админа к тому, чтобы доступ
  открыли не тому человеку. Теперь `steam_id` попадает на строку только из ответа
  Steam, и `/api/whitelist/request` тела больше не читает.
- **Аккаунт может не иметь ни email, ни пароля** — у пришедшего из Steam их взять
  негде. Обе колонки стали nullable, а `users_has_a_way_in` следит, чтобы у строки
  остался хоть один способ входа: пара email+пароль или `steam_id`.
- **Ник берётся из публичного XML профиля** (`/profiles/<id>/?xml=1`, ключ не нужен)
  при создании аккаунта — и ещё раз при входе, если в базе до сих пор стоит заглушка
  `Викинг <5 цифр>`. Это удобство, а не условие: приватный профиль или молчащий Steam
  оставляют заглушку, вход от этого не ломается. Второй заход за ником появился
  12.09.2026 после того, как сайт встретил хозяина как «Викинг 08760»: аккаунт заводит
  `src/bootstrap.js` при первом старте, и Steam в тот момент имени не дал, а потом
  спросить его было уже негде. Заменяется **только заглушка** — выбранное человеком имя
  не трогаем. Ники Steam проходят чистку от невидимых символов: имя из одних zero-width
  пробелов выглядело бы в списках пустым.
- **`adminlist.txt` требует ДВЕ формы одного номера** — `V_<SteamID64>` и
  `Steam_<SteamID64>`, обе. `ZNet.PlayerIsAdmin` сравнивает id сырым, без
  `FilterPlatformUserID`, поэтому совпадает только с той формой, которую прислал
  клиент, то есть `Steam_…`; от этой проверки зависит кнопка кика в игровом списке
  игроков. Всё остальное, что читает список, идёт через `ListContainsId` и видит
  только `V_…`. Напишешь одну форму — часть прав молча не появится.
- **Пустой `adminlist.txt` безопасен, в отличие от пустого `permittedlist.txt`.**
  Он означает «админов нет», а не «админы все», поэтому выгрузка без записей
  отдаётся, а не отклоняется: снять права тоже надо уметь. Кнопка при этом
  предупреждает, что файл снимет админку со всех.
- **Админку в игре выдаёт только суперадмин.** Кто может её выдать — может выдать
  себе, а это спавн, бан и кик. Обычный админ сайта отвечает на заявки, но кнопок
  админки не видит и получил бы 403. С 12.09.2026 это явные кнопки «Дать админку» /
  «Забрать админку» в строке игрока (раньше была галочка «в игре», её не замечали), с
  подтверждением и тостом о том, записался ли список на сервер. **Дать** можно только
  тому, у кого есть доступ (сервер отвечает 409): админ, которого не пускают, — это
  право, которое сработает в день, когда список опустеет. **Забрать** — у любого, у
  кого она есть: такие забытые админы без доступа уже встречаются, и снимать их надо
  уметь.
- **Ручной ввод номера остался ровно в одном месте — у админа**, в форме «Добавить
  в вайтлист». Поэтому ответ возвращает никнейм из Steam, и админка его показывает:
  опечатка в номере всплывает как чужое имя, а не как тишина.
- **`steam_id_verified` отличает подтверждённый номер от вписанного руками.**
  Steam подписался — `true`; админ ввёл — `false`, и так и написано и в кабинете, и в
  таблице. Флаг переключается сам, когда владелец впервые входит через Steam:
  номер наконец приходит подписанным.
- **ID лежит в базе без префикса**, `V_` приписывается только при выгрузке
  (`src/steamId.js`). Почему годится ровно `V_<SteamID64>` — выше, в разделе
  про сервер.
- **SteamID64 нельзя пропускать через `Number`.** База индивидуальных аккаунтов
  76561197960265728 уже больше 2^53, и любой круг через float сдвигает последние
  цифры, то есть пускает соседний аккаунт. Разбор и границы считаются на `BigInt`,
  дальше id живёт строкой.
- **Выгрузка отказывается отдать файл без записей** — 409, а не пустой файл.
  Пустой `permittedlist.txt` означает «пускать всех», так что файл из нуля
  одобренных заявок открыл бы сервер всему интернету — ровно наоборот к тому,
  зачем его собирали.
- **Комментариев в выгружаемый файл не кладём.** Игра пишет их в свой файл сама и
  почти наверняка не считает записью — иначе её собственный свежий файл никого бы
  не пускал, — но на 1.0 это не проверялось, а ошибиться здесь значит снова
  открыть сервер. Дешевле не проверять, чем проверять собой.
- **Привязка другого Steam сбрасывает ответ по вайтлисту в `none`.** Прежнее решение
  было про прежний номер. Не в `pending`: попросить доступ — осознанное действие.
- Один Steam — один аккаунт: `steam_id` уникален. Залогиненный игрок, привязывающий
  чужой уже занятый номер, получает отказ, а не перехват аккаунта.

## Поиск и превью ссылок

**С 13.09.2026 публичные страницы собирает сервер** (`src/seo.js`), а не скрипт в
браузере. Раньше `client/app.js` после загрузки подставлял заголовок, тексты разделов и
статьи, и тот, кто скриптов не исполняет, — Яндекс через раз, Telegram, VK и Discord
никогда — видел заглушки панели: «О нашем блоге», «г. Вильнюс, ул. Разработчиков». А
вкладка называлась просто «Astvard»: `loadSettings` затирал `<title>` названием сайта.
Слова «Valheim» не было ни в заголовке, ни в описании. Всё это видно на боевом до
выкатки, 13.09.2026.

- **Сайт — не одна страница, а шесть** (19.09.2026). Было: одна главная с якорями, и
  каждая новая тема делала её длиннее — раздел нельзя было дать ссылкой, у него не было
  ни своего заголовка в выдаче, ни своего описания, а «Что умеет мод» раздул страницу до
  79 КБ. Стало: `/` (витрина и статус серверов), `/server` (руны и общие постройки),
  `/mod` (что умеет мод), `/join` (как попасть), `/news` (лента), `/about` (о проекте и
  контакты). Список страниц — `PAGES` в `src/seo.js`: адрес, кусок разметки, пункт меню,
  заголовок и описание для поиска; **добавить страницу — это строка там и файл в
  `src/templates/pages/`**, маршрут и карта сайта подхватят сами.
  - **Оболочка одна** — `src/templates/layout.html`: шапка, меню, подвал, скрипты.
    `{{{content}}}` — кусок страницы, `{{{nav}}}` — меню, `{{{scripts}}}` — скрипты именно
    этой страницы (ленте нечего оживлять). `client/index.html` больше нет.
  - **Кусок заполняется раньше оболочки**: подставленное значение второй раз не
    просматривается, иначе текст статьи с двойными скобками стал бы полем шаблона.
  - **Меню строит сервер**, с `active` и `aria-current` — подсветка нужна и тому, кто
    скриптов не исполняет, а ссылки ведут на адреса, а не на якоря.
  - **Один адрес на раздел**: `/mod/` уводит 301 на `/mod`, `/index.html` — на `/`.
    Старые ссылки с якорями (`/#mod`, `/#articles-section`) переводит `MOVED_SECTIONS` в
    `client/app.js` — их успели раздать в Discord.
  - **Проверка чужих адресов в CSP ходит и по `src/templates`** (тест в
    `test/http.test.js`): разметка публичных страниц теперь там, и новый внешний скрипт
    иначе прошёл бы мимо неё незамеченным.
  - **«Скачать мод» — кнопка в меню, на каждой странице** (19.09.2026), и по ней окно с
    выбором: архив с GitHub или менеджер модов. Адреса — те же настройки
    `mod_download_url` и `thunderstore_url`, что у кнопок на `/mod` и ссылок в подвале;
    **обе ссылки лежат в разметке**, скрипт только показывает окно, так что оно работает
    и при мёртвом API. Не задан ни один адрес — нет ни кнопки, ни окна: кнопка,
    открывающая пустое окно, хуже её отсутствия. Само окно теперь общее для сайта
    (`.site-modal`, было `.feature-modal` у плиток мода) — и плитки, и выбор открываются
    одним и тем же кодом.
- **Куски страниц — шаблоны.** `{{ключ}}` экранируется, `{{{ключ}}}` идёт как
  есть, незаполненный ключ остаётся в странице видимым. Тест проверяет, что в ответе
  нет ни одного `{{`, поэтому **двойных фигурных скобок в комментариях шаблона не
  писать** — на этом он упал в первый же прогон. Тексты — из настроек; пустая
  настройка берёт значение из `DEFAULT_SETTINGS` в `db.js`, пустой `<title>` хуже
  стандартного. Подстановку в `app.js` не возвращать: поисковик снова увидит одно, а
  человек другое. Файл открывается только через сервер: `/index.html` уводит на `/`.
- **Заголовок и описание для поиска — свои настройки**, `seo_title` и `seo_description`
  («Настройки → Продвижение в поиске»), а не hero. Заголовок читают в списке из десяти, и
  «Valheim» в нём должен стоять в первых словах; H1 на главной — `hero_title`, это текст
  хозяина, его не трогали. Шаг «Попроси доступ» в блоке «Как попасть» говорит, что будет
  с заявкой сейчас, по `whitelist_auto_approve`. Названия пунктов меню в шаге «Добавь
  сервер» — строки самой игры (`Начать игру`, `Присоединиться к игре`, `Добавить сервер`),
  найдены в `valheim_Data/resources.assets` клиента, не по памяти.
- **У статьи свой адрес**: `/news/<id>-<транслит заголовка>`. Решает только id: старый
  заголовок, слэш в конце, адрес без слов — 301 на текущий, и `utm_*` из запроса едут
  следом. Черновик — 404. Модалка на главной убрана, карточка — одна ссылка на заголовке,
  растянутая на всю карточку. `articles.updated_at` (в схеме дважды) идёт в
  `sitemap.xml` и в `dateModified`; у статей, написанных до колонки, там `NULL`, и берётся
  `created_at`.
- **`robots.txt`, `sitemap.xml` и canonical строятся от `APP_URL`**, не от `Host`.
  `/api/public/` в robots открыт: из него главная рисует блоки игры, и поисковику,
  исполняющему скрипты, их надо видеть. `Clean-param` склеивает для Яндекса адреса с
  метками рекламы.
- **Кабинет, вход, админка и API отвечают `X-Robots-Tag: noindex, nofollow`**, а в
  robots.txt не закрыты: закрытое там поисковик не открывает и noindex не видит, а адрес,
  известный по ссылке, всё равно попадает в выдачу голым. Список — `isPrivatePath`.
- **`www` и весь http уводятся одним 301 на `https://astvard.online`** в nginx: иначе это
  две копии каждой страницы. Раньше http://www вёл на https://www, и там отвечал тот же
  сайт. Сертификат `www` покрывает, certbot продлевает через nginx-плагин. Конфиг nginx
  кладёт `install-site.sh`, `update-site.sh` его не трогает.
- **Метрика и коды подтверждения** — настройки `yandex_metrika_id`,
  `yandex_verification`, `google_verification`. Вставлять можно то, что показал сервис,
  хоть тег или код счётчика целиком: сервер вынимает номер или код и не вставляет ничего,
  что на них не похоже, — значение уходит в `<head>` каждой страницы. Счётчик встаёт на
  все страницы сайта, кроме админки, и только тогда CSP этих страниц пускает
  `mc.yandex.ru`. Цели для рекламы: `steam_login` — клик по любой «Войти через Steam»,
  `whitelist_request` — удачная заявка в кабинете; в Метрике их заводят как
  «JavaScript-событие» с этими именами. Вебвизора нет намеренно.
- **CSP не пускала Google Fonts**, и сайт шёл системным шрифтом: в консоли боевого
  `Loading the stylesheet 'https://fonts.googleapis.com/…' violates … "style-src 'self'
  https://cdn.quilljs.com 'unsafe-inline'"`. Теперь `fonts.googleapis.com` и
  `fonts.gstatic.com` в списке. Lucide на главной закреплён на версии и идёт с `defer`:
  `@latest` — это редирект на каждый заход и чужой релиз, который может переименовать
  иконку за ночь.
- **Редактор статей молча не запускался из-за CSP** (замечено 13.09.2026). Админка брала
  Quill с `cdn.quilljs.com`, который был в CSP, но этот адрес теперь отвечает 301 на
  `cdn.jsdelivr.net`, а CSP судит тот адрес, куда увела переадресация. Quill не
  загружался, `public/app.js` подменял редактор голым полем, и статьи сохранялись
  простым текстом без разметки — в консоли два нарушения CSP, больше нигде ничего. Теперь
  Quill берётся прямо с jsdelivr, и CSP пускает не весь jsdelivr, а только путь
  `npm/quill@1.3.6/` (`QUILL_CDN` в `server.js`). Меняешь версию в `public/index.html` —
  меняй и путь: тест в `test/http.test.js` сверяет внешние скрипты и стили страниц с CSP.
  Переадресацию на чужой адрес тест без сети не поймает, её видно только в консоли браузера.
- **Раздел «Что умеет мод»** (18.09.2026, с 19.09 — страница `/mod`, `src/templates/pages/mod.html`, `client/mod-features.js`):
  22 плитки в четырёх группах — «С чего начать», «Постройки», «Рельеф», «Функции», — и у каждой
  своё окно. **Текст окон лежит прямо в разметке**, в скрытом `.feature-texts`, а не приезжает
  запросом: так его читает поисковик и человек без скриптов, и окно открывается при мёртвом
  сервере. Скрипт только переносит нужный `#feature-<id>` в окно, Esc и клик по фону закрывают.
  Разметку собирал одноразовый генератор, дальше правится руками; id плитки и id текста обязаны
  совпадать, иначе окно молча не откроется. Содержимое снято с кода мода (`Plugin.PlayerRules.cs`,
  меню игрока), а не с этих заметок, и **всё, что правило может изменить, написано как «обычно»
  и «решает админ»** — живые значения лежат на сервере, а не в тексте. Меняется мод — раздел
  устаревает молча: тестов на смысл текста нет, только на отсутствие незаполненных мест.
  Половина описанного **в игре ещё ни разу не проверена** (см. «Незакрытое»): это описание
  задуманного, а не отчёт о работающем.
- **Иконки растровые**: `favicon.ico`, `apple-touch-icon`, манифест — из `tools/icons/`,
  картинка превью — из `tools/og-image/`, там же команды. Яндекс в выдаче показывает
  `/favicon.ico`, не SVG.
- **Что может сделать только хозяин** — нужен его аккаунт: добавить сайт в Яндекс
  Вебмастер и Google Search Console, вставить код подтверждения в настройки, отправить
  там `https://astvard.online/sitemap.xml`; завести счётчик Метрики и две цели.
- **Выкачено и проверено на боевом 13.09.2026.** `robots.txt` и `sitemap.xml` отдают
  адреса `https://astvard.online`; `http://www`, `https://www` и голый http — одним 301 на
  `https://astvard.online` с тем же путём и `utm_*`; `/news/1` — 301 на
  `/news/1-dobro-pozhalovat-v-astvard`, черновик — 404; `noindex` стоит на кабинете,
  входе, админке и API и не стоит на главной; в разметке главной и статьи нет ни `{{`, ни
  заглушек, JSON-LD разбирается; шаг «Попроси доступ» пишет «принимаются сразу» — на
  проде автоприём включён. Шрифт Inter грузится (400–800), новых нарушений CSP в консоли
  нет. Мод игрового сервера после перезагрузки nginx забирает списки с тем же 200.
  Метрика и коды подтверждения не заданы — ждут хозяина, поэтому счётчик и его CSP в
  живую не видели, только в тестах.

## Постройки для игроков: сайт и мод вместе

**С 13.09.2026 правила для игроков и доступ к общим постройкам меняются и в игре, и на
сайте** («Админка → Постройки»). Раньше и то и другое жило только в моде: правила в его
конфиге, доступ — строкой `#players yes` в файле шаблона, а менять можно было только из
игровой панели. Теперь хранит сайт (`src/gameBuilds.js`, таблицы `game_build_rules`,
`game_build_rule_meta`, `game_template_access`, `game_build_sync`), а мод
(`Plugin.SiteBuilds.cs`) раз в `[Сайт] BuildsSeconds` (15 с) забирает состояние
`GET /api/game/builds?rev=N` по тому же токену, что и списки доступа, и отправляет
изменённое админом в игре `POST`-ом туда же. Адрес — `[Сайт] BuildsUrl`; пустой значит
«рядом со списками»: `ListsUrl`, где `lists` заменено на `builds`, так что серверу, который
уже забирает списки, новая строка в конфиге не нужна.

- **«Действует последнее» — это порядок, а не часы.** Каждое изменение с любой стороны
  берёт следующую ревизию из `game_build_sync`; мод передаёт в `rev` ревизию, которую уже
  применил, и строка с ревизией выше помечается в админке «ждёт сервер». Часы VPS,
  контейнера и браузера сверять не приходится.
- **Мод сначала отправляет, потом забирает.** Изменение из игры, которого сайт ещё не
  слышал, новее любого ответа сайта: применить ответ раньше — значит вернуть админу под
  руки старое значение. Пока очередь отправки не пуста, ответ сайта не применяется.
- **Первое знакомство — засев.** Пустой сайт отвечает `seed needed`, и мод присылает все
  свои правила и доступ из заголовков шаблонов. Засев только дописывает недостающее —
  правка, сделанная на сайте раньше, остаётся, — а второй засев игнорируется.
- **Какие правила бывают, сайт не знает сам.** Их при каждом старте описывает мод
  (`meta`: название, группа, вид, границы), и сайт проверяет значения по этим границам.
  Правило, добавленное в новой сборке мода, появляется в админке без правки сайта.
- **Доступ по имени — заголовок `#allow <SteamID64,…>`** в файле шаблона, рядом с
  `#players yes`. Сервер сверяет платформенный id сокета при каждой постройке
  (`OpenTo` в `Plugin.BuildPause.cs`), а список общих построек теперь собирается для
  каждого подключённого отдельно: игрок видит открытое ему, а чужие номера не уходят
  никому. «Разрешить игрокам» в игре трогает только «всем», выбранных оставляет;
  выбирать людей можно только на сайте. Публичная страница говорит «открыта некоторым
  игрокам» — сколько и кому, не говорит.
- **Файлы шаблонов пишет только мод**, и только когда заголовки меняются, через
  временный файл. Сайт папку конфигов мода по-прежнему лишь читает — прав на запись туда
  ему не нужно.
- **Проверено 13.09.2026 на отладочном сервере Windows с локальным сайтом**, живым
  обменом: засев (20 правил, 4 постройки); правка на сайте доехала за ~7 с — в конфиге
  `Floor = 0`, `CopyMax = 12`, пауза 3 мин, в «Плавильне» `#players yes` и `#allow`
  сразу после первой строки; «Не для всех» и снятая галочка в «Кому…» через интерфейс
  убрали оба заголовка; «ждёт сервер» сменилось на «применено» само. **Не проверено**:
  изменение из игры (нужен клиент) и постройка игроком, которому постройку открыли по
  имени.
- **На VPS включено 13.09.2026 в 21:27 UTC** сборкой `6bbd7dac`. Перезапуск ждал
  последнего игрока разовой службой `systemd-run` (лог без его выхода плюс живой
  UDP-трафик на 2456 — tcpdump показал ~230 пакетов в секунду от клиента, который по
  логу ещё «в игре»; остановка сразу после ухода, мир сохранён). Мод засеял сайт —
  20 правил ровно со значениями из конфига сервера и 3 постройки — и дальше спрашивает
  раз в 15 с (`GET /api/game/builds?rev=5` → 200 в журнале nginx), списки доступа — как
  раньше, раз в минуту. `BuildsUrl` в конфиге пуст и вывелся из `ListsUrl`. Хозяин за
  20 минут до подключения сервера уже нажимал «Открыть всем» / «Не для всех» у двух
  построек: засев эти правки оставил (`updated_by` — его ник, ревизии 2 и 4) и дописал
  только третью постройку; заголовки в файлах с этим уже совпадали, мод ничего не
  переписал. Публичная страница показывает то же.
- **Встроенных построек в моде больше нет** (14.09.2026). С 08.09 мод при каждом запуске
  записывал в папку шаблонов пять построек хозяина из `BuiltInTemplates.cs` («Стартовый
  дом №1», «Плавильня», «Угольные печи», «Полная кухня», «Платформа 4х4») — не только
  себе, а каждому, кто поставил мод. Хозяин заметил их у игроков в «Шаблонах»: при
  открытом «Копировании» и выключенных «Ресурсах с игроков» они ставились даром и без
  паузы. Теперь шаблон берётся только с сервера («Шаблоны сервера», открытые игроку) или из
  собственной папки игрока. Уже записанные у игроков файлы мод не трогает: это их папка,
  и отличить свой изменённый шаблон от посеянного честно нечем.
- **Локальный стенд без `VALHEIM_MOD_CONFIG` в `.env`** не видит файлы шаблонов и пишет у
  каждой постройки «сайт не видит файл» — это не сломанный обмен: кнопки работают,
  доступ хранится по имени. На отладочном сервере для обмена стоят `BuildsUrl` на
  `http://127.0.0.1:3001/api/game/builds` и `ListsToken`, а `ListsUrl` пуст — чтобы мод
  не переписывал локальные списки доступа данными локальной базы.

## Сборка и раскладка мода

DLL кладётся **в две папки**, и обе должны совпадать байт в байт. Разные версии
на клиенте и сервере дают тихие рассинхроны, которые выглядят как «кнопка не работает».

**С Thunderstore одинаковой DLL у всех больше не будет — и это выбор хозяина**
(16.09.2026). Игроки ставят мод из менеджера и обновляются когда захотят, а проверок,
которые не пускают на сервер, в моде нет намеренно: заходят и без мода, и со старой
версией. Правило «байт в байт» остаётся про наши три установки — клиент,
Windows-сервер и VPS. У игроков версия может быть любой, поэтому формат обмена обязан
терпеть старого клиента; если терпеть больше нельзя — это новая мажорная версия, и
только она. Расхождение при этом не молчит: сервер сообщает свою версию в ответ на
обычный запрос панели, клиент один раз за подключение пишет строку в чат. Версия живёт
одной константой `Version` в `Plugin.cs`, `manifest.json` обязан её повторять, а
пакет и правила — в `tools/thunderstore/README.md`.

```bash
dotnet build valheim-mod/AstvardServerMod/AstvardServerMod.csproj -c Release
```

Собранное лежит в `bin/Release/net472/` — **не в Debug**, туда никто не смотрит.
Копировать в обе:

- `C:\Games\steamapps\common\Valheim\BepInEx\plugins\`
- `C:\Games\steamapps\common\Valheim dedicated server\BepInEx\plugins\`

Сверять всегда хешем, а не датой:

```bash
md5sum valheim-mod/AstvardServerMod/bin/Release/net472/AstvardServerMod.dll \
       "/c/Games/steamapps/common/Valheim/BepInEx/plugins/AstvardServerMod.dll" \
       "/c/Games/steamapps/common/Valheim dedicated server/BepInEx/plugins/AstvardServerMod.dll"
```

Игру и сервер надо перезапускать — DLL читается один раз при старте.

**С 11.09.2026 есть третье место — сервер на VPS**,
`/srv/valheim/server/BepInEx/plugins/`. Клиент у хозяина один на оба сервера, так
что DLL на VPS должна совпадать с клиентской всякий раз, когда играют там; сейчас во всех
трёх местах `c1482aa2` — версия 1.2.5 от 19.09.2026, против Jotunn 2.30.1 (`6b5dbb50`).
На VPS она легла в 15:21 UTC: сервер остановлен `docker compose … stop` (мир записан,
`World save (5/5) done`, сохранение № 846), DLL подменена, поднят обратно — в логе
`Loading [Jotunn 2.30.1]`, `Loading [AstvardServerMod 1.2.5]`, `Kept zones`, ни одной
ошибки мода. Прежняя отложена как `/srv/valheim/AstvardServerMod.dll.107a18a7.bak`.

Что в 1.2.5 сверх 1.2.4: «в ряд» ставит один сундук вместо пары, сундук на земле встаёт по
сетке игры, пометка сундука не переживает спринт, «Перепроверить» не приписывает себе ходы
обычного разбора и отвечает сразу, когда работать негде, и правило «Сундуки» спрашивается
там, где делается дело. Это пять находок разбора, устоявших под опровержением, плюс просьба
хозяина про один сундук.

Роздано наружу: релиз `v1.2.5` на GitHub (архив собирает `tools/release/build.py`) —
**скачанный оттуда архив проверен, DLL внутри та же, `c1482aa2`**, и Jotunn та же,
`6b5dbb50`. Пакет для Thunderstore (`tools/thunderstore/build.py`) собран как
`AstvardServerMod-1.2.5.zip` и ждёт хозяина: заливка идёт под его аккаунтом.
**Листинг на Thunderstore с 19.09.2026 в отказе, и мода там не видно** — проверено живым
запросом 19.09.2026: `api/experimental/package/Astvard/AstvardServerMod/` отдаёт
`community_listings: valheim → review_status: rejected`, последняя залитая версия там 1.2.1,
страница пакета анонимному отвечает 302 и дальше 404, поиск по слову Astvard не находит
ничего. Живой остаётся только прямая ссылка на скачивание версии. Подробности и что с этим
делать — `tools/thunderstore/README.md`; оспорить отказ может только владелец аккаунта.
Прежние сборки остаются на VPS в `/srv/valheim` как `AstvardServerMod.dll.<md5>.bak`:
`107a18a7` — 1.2.4, `83d028fc` — 1.2.1, `bc843cf3` — она же 1.2.0, но с шестью коммитами
после релиза (в
заметках тут стояло `de86ea74`, и это был как раз тот случай, когда разложили, а запись не
поправили), `a8f50c62` — 18.09 под номером 1.1.0, `0f8811a8` и `95c6bcf9`.

**Раскладывает DLL одна сессия, и хозяин говорит какая** (19.09.2026). В этой папке
работают две, и обе собирали и копировали в одни и те же `BepInEx/plugins`, перетирая друг
друга: разложенное одной жило до первой сборки другой, а хеш в этих заметках устаревал
быстрее, чем его успевали записать. Поэтому раскладка и перезапуск отладочного сервера —
за той сессией, которой хозяин это сказал; остальные собирают у себя, коммитят и **в
игровые папки не копируют**. Если не помнишь, сказали ли это тебе, — значит не тебе:
спроси, а не раскладывай. Правило про чистое дерево выше от этого не отменяется, оно про
другое — про то, что попадает в сборку.

**Собирать для выкладки — только из чистого дерева** (19.09.2026). В этой папке иногда
работает вторая сессия, и `dotnet build` заберёт её незакоммиченные правки молча: 1.2.4,
собранная в 17:47, унесла в себе чужой `Plugin.ChestRow.cs`, правленный в 17:42, и на VPS
уехало бы то, чего нет в git. Поймалось сравнением времени файлов. Порядок: `git worktree
add <папка> <коммит>`, собрать там, сверить `md5sum` — сборка из дерева с чужими правками
даёт другой хеш, и это единственный признак.

**Но и хеш сам по себе — не доказательство совпадения исходников: он зависит от пути.**
Тот же код из `…/clean-build` дал `107a18a7`, а из `…/release-build` — `d155cf2b`:
компилятор пишет пути в сборку. Поэтому архив для релиза собирается **не заново, а из той
самой DLL, что разложена на серверах** — она копируется в `bin/Release` перед
`tools/release/build.py`. Иначе наружу уедет файл, которого никто не запускал, а заметка
«везде одно и то же» станет неправдой в тот же день.

**Раскладывать приходилось под запущенной игрой**, а Windows переписать открытый файл не
даёт — зато даёт переименовать. Отодвинутые так сборки лежат рядом, в тех же папках, как
`AstvardServerMod.locked-20260919b.bak` и `…-c.bak`. Работающая игра при этом остаётся на
прежней сборке: DLL читается один раз при старте, и новая начинается с их следующего
запуска — на Windows-сервере это 19.09 в 17:45, на VPS в 14:59 UTC.
В этих же трёх папках лежит и сама `Jotunn.dll`, и её версия должна совпадать так же:
мод собирается против той, что лежит у **выделенного сервера** (`HintPath` в csproj), а
работает у клиента. Новую DLL — `scp` в эту папку, `chown valheim:valheim`,
`md5sum` там же, затем `docker compose -f deploy/valheim/compose.yml restart` из
`/srv/astvard` (restart останавливает тем же SIGINT, мир пишется).
Если на сервере могут играть, вместо `restart` — `deploy/valheim/restart-when-empty.sh`: DLL
кладётся рядом как `AstvardServerMod.dll.new`, скрипт ждёт, пока ни лог, ни порты не покажут
игроков, подменяет её, прежнюю оставляет в `/srv/valheim` как `.bak` и поднимает сервер; как
запускать — в его шапке, ход — в `/root/astvard-restart-status.txt`. **Запуск по строке
`Game server connected` вслепую не ждать:** пока новая игра не написала своё, в файле строки
прошлого запуска, и ожидание «дожидалось» сразу — так обманулись 13.09.2026. Скрипт сверяет
время строки с часами контейнера: в логе его местное время, не время машины.

Тесты геометрии игры не требуют и идут за секунды:

```bash
dotnet test valheim-mod/AstvardServerMod.Tests
```

Они компилируют `Geometry.cs` внутрь себя (`<Compile Include>`), а не ссылаются на
мод: мод собирается только там, где установлен Valheim, а тесты должны идти везде.
Поэтому **вся математика, которую хочется проверить, должна жить в `Geometry.cs`** —
файле, который не знает ни про Unity, ни про игру.

## Как здесь принято

- **Комментарии и коммиты — по-английски. Всё, что видит игрок, — по-русски.**
  Не смешивать. UI-строки в коде русские, комментарий рядом с ними английский.
- Комментарий объясняет **почему**, а не что. Строку, пересказывающую соседний код,
  лучше не писать.
- Сообщение коммита — обычная фраза без префиксов вроде `feat:`, а тело объясняет
  причину и чем это было раньше. Примеры в `git log`; держись той же интонации.
- Заканчивать коммит строкой `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.
- **Кнопки и ссылки — классами панели, а не своими.** Прежняя система
  (`.btn`/`.link-*` в `frontend/style.css`) уехала вместе со старым сайтом. У панели
  свои: `.btn` (она же основная), `.btn-secondary`, `.btn-danger`, `.btn-sm` для кнопок
  в строке таблицы, `.btn-save` в кабинете, `.badge`/`.badge-success`/`-warning`/
  `-danger`/`-muted` для статусов, `.data-table` у таблиц, `.card` в кабинете,
  `.check-chip` для галочки размером с палец, `.toolbar-form` для формы над таблицей.
  Смотреть соседний раздел и брать оттуда; новый вид — вариант в общих стилях, а не
  правило под один id. `.btn-primary` не существует: основная кнопка — это `.btn`.
- **Таблица на телефоне — это карточка, и класса `cards-mobile` мало.** Механизм
  держится на трёх вещах сразу: класс на таблице, разметка ячеек (`mobile-primary` —
  то, по чему строку узнают, `mobile-hidden` — что прячется, `no-label` — колонка
  действий, `empty-state` — заглушка) и тап по строке, открывающий полную строку в
  общей модалке `showRowDetail(заголовок, [[метка, значение]], кнопки)`. Поставишь
  только класс — на ≤640 px шапка исчезнет, строка станет flex-карточкой, а
  непомеченные ячейки уедут за край без полосы прокрутки: `.table-container` режет
  молча, а `.table-scroll` на этой ширине отключает прокрутку сам. На этом обожглись
  12.09.2026: на телефоне нельзя было ни одобрить заявку, ни удалить сервер, и
  выглядело всё целым. Кнопки, спрятанные вместе с `no-label`, обязаны появляться в
  футере модалки — те же классы, тот же делегированный обработчик.
- Работа идёт в ветке `valheim-mod`, не в `main`.
- **Правки делаются здесь и уезжают через GitHub. На VPS не коммитить и не править
  файлы репозитория.** Порядок один: правка на машине → коммит → `git push` →
  `/srv/astvard/deploy/update-site.sh` на VPS. Обновление делает `git reset --hard
  origin/valheim-mod`, то есть любая правка, сделанная на сервере, пропадёт молча и в
  самый неудобный момент — а «на проде поправлю и потом перенесу» так и не переносится.
  Вне git на VPS живут только `.env`, `deploy/valheim/.env` и `node_modules`.

## Грабли этого окружения

Не project-specific, но каждая из них однажды дала молчаливо неверный результат.

- **Кириллица в `curl -d '...'` портится.** Аргумент командной строки до curl доходит
  битым. Класть тело в файл и слать `--data-binary @file`. (Через heredoc в файл
  кириллица проходит нормально — проверено, UTF-8 не ломается.)
- **`taskkill` внутри составной команды Bash может «успешно» ничего не сделать.**
  Убивать процессы через PowerShell и обязательно проверять, что процесса больше нет,
  прежде чем измерять что-либо после.
- **`strings` в Git Bash отсутствует.** Поиск по бинарнику молча вернёт пустоту,
  как будто ничего не нашлось. Искать питоном.
- **Экранирование в heredoc и `re.sub` регулярно съедает правки:** `\n` превращается
  в настоящий перенос, замена переинтерпретируется. Для скриптовых правок — `chr(92)+"n"`
  и функция-замена вместо строки; лучше же писать файл целиком. Кавычки у `<<'EOF'` не
  спасают: 11.09.2026 `\\n` в питоне из такого heredoc всё равно дошёл переносом и
  разорвал строковые литералы C#. Скрипт с экранированием — только файлом через Write.
- **`grep` в Git Bash не годится для поиска `\r`, и врёт в обе стороны.** 11.09.2026
  `grep -c $'\r'` насчитал CR в каждой строке файлов, где их не было ни одного, а в
  трубе `printf 'a\r\nb\n' | grep -c "$(printf '\r')"` не нашёл настоящий. Концы строк
  проверять питоном: `open(f, 'rb').read().count(b'\r\n')`.
- **Из worktree `preview_start` всё равно берёт `.claude/launch.json` основной папки**
  и запускает её код, а не код worktree. 11.09.2026 сайт ответил 404 на маршрут, который
  был только в новой ветке. Что запущено нужное — проверять маршрутом, которого в старом
  коде нет; для кода из worktree нужна запись с абсолютными путями в основном
  `launch.json`, которую потом убрать. 14.09.2026 было уже наоборот: правку основного
  `.claude/` инструменты из worktree-сессии не дали, а запись во **временном** `launch.json`
  самой worktree (`node --env-file=<файл с PORT=3002> <абсолютный путь>/server.js`, чтобы
  не мешать чужому серверу на 3001) `preview_start` подхватил. Какой код отвечает — всё равно
  сверять заголовком или маршрутом, а запись убрать до коммита.
- **Длинный многострочный якорь может не совпасть, хотя он в файле есть.** 12.09.2026
  замена блока README с ``` и обратным слэшем внутри дала `count == 0`, при том что
  каждый его кусок по отдельности находился ровно один раз. Не гадать: искать границы
  блока по строкам (`startswith`, номер строки) и менять срез списка строк.
- **Правки «по якорю» проверяй счётчиком совпадений.** Однажды `replace(..., 1)` попал
  во второе совпадение и засунул создание кнопок внутрь обработчика клика — кнопки
  начали размножаться при каждом нажатии. Правило: `assert src.count(old) == 1`.
- **Труба из Node в Python на этой машине портит UTF-8.** 12.09.2026 ответ API,
  пропущенный через `node ... | python -c "json.load(sys.stdin)"`, вышел в
  «РџР»Р°РІРёР»СЊРЅСЏ»: питон читает stdin в cp1251. Данные были целы — врал замер.
  Проверять содержимое так: `curl -o файл`, затем `io.open(файл, encoding='utf-8')`.
- **Замер вёрстки сразу после смены вьюпорта врёт.** 12.09.2026: после
  `resize_window` без перезагрузки у уже отрисованных элементов каскад не
  пересчитался, и поле показало 120 px вместо 278 — почти записали в баги. А при
  скрытой панели предпросмотра кадры не идут, и CSS-переход замирает на начальном
  значении: модалка «висела» на 20 px ниже, хотя правило ставит `translateY(0)`.
  Правило: после смены ширины — перезагрузка, а геометрию мерить, погасив переходы.
- **Браузерная панель одна на всех.** Подагенты ходят в тот же Browser pane: пока
  они работают, вкладка может увести на чужой localhost, и `javascript_tool`
  выполнится не там, где ты думаешь. 12.09.2026 запрос ушёл на 8177 (стенд агента)
  вместо 3001 и вернул 501 от питоновского сервера. Проверять origin в ответе.
- Мод и сайт проверяй **эмпирически**. Живой запрос, живой сокет, живой лог. Слишком
  многое здесь выглядит рабочим и не работает.


## Раскладка сортировщика: сайт и мод

**С 19.09.2026 куда сортировщик кладёт предмет, решает админ на сайте** («Админка →
Сортировка», `src/gameSorting.js`, `Plugin.SiteSorting.cs`). Мод раскладывает по типу
предмета, и для большинства вещей этого хватает, но тип — слово игры, а не хозяина базы:
оленья шкура для игры такой же «материал», как камень.

- **Каталог присылает мод**, при каждом старте сервера: ключ перевода (`$item_deerhide`),
  русское имя, тип и та категория, которую мод дал бы сам. Сайт этого списка не знает и
  знать не должен — предметы новой версии игры появятся на странице без единой правки
  сайта. Тот же приём, что у правил построек (`game_build_rule_meta`).
- **Обратно уезжает только выбранное** — «ключ=номер», по строке. Четыреста строк «этот
  предмет там, где и был» — это килобайт в минуту без единого решения.
- **Пустой выбор — не «Разное».** Это «решает мод», то есть по типу предмета; «Разное» —
  ответ, пустой выбор — его отсутствие. В базе это `NULL` против `0`, и путать их значит
  молча менять раскладку на всей базе.
- **Дальше сервер раздаёт выбранное клиентам** (`AstvardSortKinds`), потому что сортировщик
  работает у них; токен сайта есть только у сервера. Раздача едет на том же обходе пиров,
  что и зоны, и только когда изменилось.
- Адрес выводится из `ListsUrl`, как у построек: `[Сайт] SortingUrl` пуст — значит
  `lists` → `sorting`, тем же токеном. Раз в минуту (`SortingSeconds`).
- Таблицы — `game_sort_items` и `game_sort_sync`; страница обновляется сама раз в десять
  секунд, пока открыта, как «Постройки».
- **Проверено живыми запросами на локальном стенде** (19.09.2026): пустой сайт отвечает
  `seed needed`, каталог принимается (`ok 3`), выбор поднимает ревизию, мод получает
  `rev 1` и строку выбора, снятие выбора убирает её, чужая категория и незнакомый предмет
  отклоняются, запрос без токена — 401. С 19.09 это ещё и два теста в `test/http.test.js`,
  включая то, что каталог от мода не стирает выбор админа. **Не проверено**: весь мод
  целиком — ни каталог из игры, ни раздача клиентам в игре не запускались ни разу.

**Страница «Сортировка» рассчитана на тысячу строк** (19.09.2026). На боевом в каталоге
982 предмета, и первая её версия — плоская таблица со списком у каждой строки — была
нерабочей: выбрано за все дни ноль. Что изменилось:

- **Полки над таблицей — перепись и фильтр разом.** Число на полке считается по итогу
  (выбор админа, а где его нет — решение мода по типу), потому что стену сундуков готовят
  под итог. Сразу видно перекос: «Оружие 238» и «Инструменты 10». Нажатие оставляет в
  таблице одну полку, повторное снимает фильтр.
- **Пачкой, одним запросом и одной ревизией** (`PATCH /api/admin/sorting/items`): отметить
  найденное, выбрать полку, подтвердить. Тридцать ревизий подряд значат для мода ровно то
  же, что одна, — он забирает выбор целиком, — зато «ждёт сервер» гаснет разом. Незнакомый
  ключ пропускается молча, в ответе — сколько строк легло.
- **Фильтры:** «решает сервер / выбрано руками» и тип предмета игры; поиск ищет и по типу.
  Перевод типов (`SORTING_TYPES` в `public/astvard.js`) — только для выпадающего списка, и
  незнакомый тип показывается как есть: новая версия игры добавит строку без перевода, а
  не сломает страницу. Сверялся он по самим предметам с боевого, а не по памяти.
- **Кто и когда выбрал** — эти поля приходили с сервера и выбрасывались страницей.
- **На телефоне — карточки**, как в остальных таблицах. Галочка остаётся видимой (иначе
  пачку не собрать), полка уходит подписью под имя, а полка-колонка и список прячутся: у
  ячейки «Сейчас» нет сжатия, и имени доставалось 46 px из 317. Список полок открывается
  тапом по карточке, в модалке.
- **После сохранения перерисовывается одна строка**, а не тысяча, и поиск ждёт 150 мс
  после последней буквы.
- **Смотреть панель, не входя в неё, можно стендом**: та же разметка из `public/index.html`
  и тот же кусок `public/astvard.js`, вокруг — заглушки `escapeHtml`, `showToast`,
  `confirmDialog` и подменённый `fetch` с поддельным каталогом. Стенд кладётся в папку
  черновиков и отдаётся `python -m http.server`: из `file://` браузерная панель работать не
  даёт («static snapshot»), а `.app-container` — сетка в две колонки, и без пустого
  `<aside class="sidebar">` страница встаёт в колонку 260 px и выглядит сломанной. Так
  найден и разобранный выше промах с шириной имени. **Живьём, с настоящим входом в
  панель, страницу по-прежнему не видели.**

**Постройку можно настроить и убрать с сайта** (19.09.2026). В «Админке → Постройки» у
каждой постройки появилась кнопка «Настроить»: имя, категория и «Убрать постройку» с
подтверждением. Сам сайт при этом по-прежнему **ничего не пишет** в папку мода — прав у
него там нет и не будет.

- **Просьба едет поручением с номером** (`game_build_jobs`): `job<TAB>id<TAB>вид<TAB>имя<TAB>значение`
  в том же ответе, что и правила с доступом. Мод делает и возвращает `done<TAB>id` своим
  обычным каналом. Номер снимает поручение — не «мод что-то прислал», а именно номер:
  иначе сделанное не отличить от потерянного.
- **Поручения не прячутся за «ничего не менялось»**: пока очередь не пуста, ответ всегда
  полный. Мод, применивший ревизию, но не осиливший поручение, иначе не получил бы его
  никогда.
- **Мод отвечает `done` и когда делать было нечего** — файла уже нет, имя занято, папка не
  читается. Поручение, которое не снимается, сайт будет слать вечно, и в логе это выглядит
  как работа.
- **Переименование двигает и файл**: имя файла выводится из названия. Занятое имя — отказ,
  иначе одна постройка затёрла бы другую. Сайт следом переносит строку доступа: она лежит
  под именем постройки, а у переименованной оно другое.
- **Удаление не стирает**: файл уезжает в подпапку `deleted`, как и при удалении из игры.
- Заголовок в файле правится `SiteSync.WithHeader` — только в шапке, до первой не-`#`
  строки: `#name` среди координат деталей это имя детали, а не заголовок. 13 тестов.

**Поля во всей панели и на сайте выглядят одинаково** (19.09.2026). Вид жил на классах
`.form-control` и `.form-input`, а класс надо помнить — поиск на «Сортировке», написанный
без него, встал белой коробкой посреди тёмной страницы. Теперь вид у самих `input`,
`select` и `textarea`, в панели и на публичных страницах со своими цветами; классы
остались и по-прежнему нужны — они задают ширину. Из правила по именам исключены кнопки,
галочки, радио, выбор файла и `select`-ы редактора статей: у Quill из них собрана панель
инструментов.

**Разбор мода 19.09.2026: что нашлось и что из этого починено.**

- **Повозку нельзя было назначить ни на подачу, ни на сбор** — и молча. `HasChestFlag` в
  `Plugin.Automation.cs` спрашивал `container.GetComponent<ZNetView>()`, а у повозки
  `Container` на дочернем объекте и `ZNetView` на корне; ровно та яма, что однажды
  оставила повозку неразобранной у сортировщика. Заметка об этом в списке была, а код —
  нет. Теперь везде `ViewOf` (вверх по объекту).
- **Два обращения к владельцу сети шли без проверки на null** (`TryStoreNearby`,
  `TakeFrom`): сегодня туда не попасть — повозка не проходила пометку, — но стоило
  починить пометку, и первое же исключение унесло бы **всю автоматику до конца сессии**.
  Починены вместе, иначе одно чинило бы во второе.
- **Поиск сундука в детали делался тысячами раз в секунду.** `GetComponentInChildren<Container>()`
  — рекурсивный обход префаба, а звали его трижды: автоматика раз в секунду, сортировщик
  раз в секунду, подписи дважды. На базе в полторы тысячи деталей это порядка шести тысяч
  обходов в секунду, и почти все — чтобы узнать, что контейнера нет. Теперь ответ помнится
  на деталь (`ContainerOf`), в том числе ответ «нет»; словарь чистится от снесённых
  деталей, когда перевалит за 4096 — зоны грузятся и выгружаются весь вечер.
- **Контур зоны сундуков считался каждый кадр**: 72 точки, у каждой спрошена высота земли,
  то есть больше четырёх тысяч запросов к физике в секунду, пока подсветка просто включена.
  Теперь — когда игрок отошёл на треть метра или поменял саму зону. У контура поставленной
  зоны такая же защита была с самого начала.
- **Любой сбой внутри посекундного прохода уносил работу до конца сессии.** Это не
  предположение: так мод уже терял автоматику, когда контракт RPC разъехался с игрой.
  Оба цикла — сортировщика и автоматики — теперь ловят исключение, жалуются **один раз на
  причину** и идут дальше. И то и другое ожидание (`WaitForSeconds`) заводится один раз, а
  не заново каждую секунду.
- **Ник с «|» или «=» рвал список имён** в зонах: строка имён разделена именно ими, а ник
  в Steam бывает любым. Чистится при отправке.

Что посмотрено и **оставлено как есть**: у `Plugin.Zones.cs` (серверные зоны) все списки
статические, мусора не делает; `Plan.Where` заводит список на предмет, но зовётся считанные
разы за проход; раздача зон на сервере строит по строке на игрока в секунду — на десятке
игроков это десяток мелких строк, мерить нечего. LINQ в горячих местах нет ни одного.

**Чего разбор не касался:** `Plugin.Terrain.cs`, `Plugin.RoadJob.cs`, `Plugin.Bridge.cs`,
`Plugin.Fence.cs` и прочая геометрия — они работают пачками по нажатию, а не каждый кадр,
и их цена платится один раз за постройку.

**Имена предметов в каталоге — русские** (19.09.2026, 1.2.3). Раньше на сайт уезжали
«Deer Hide» и «Barley Flour»: сервер локализует своим языком, а он английский, и оленью
шкуру нельзя было найти по слову «шкура». Все языки игра держит в одном CSV, так что мод
на время сборки каталога просит у неё русский и сразу возвращает прежний — то же самое
делает её собственный `SetLanguage` (вычистить словарь и загрузить заново), только без
записи в настройки и без события о смене языка: на выделенном сервере ни то, ни другое
никому не нужно.

- **Чистка словаря обязательна, и она закрыта** — `Localization.Clear()` идёт через
  `AccessTools`. Без неё остался бы кэш на сотню строк: `SetupLanguage` кладёт слова
  поверх, а кэш не трогает, и имя, прочитанное до переключения, вернулось бы английским.
  Пропал член — мод говорит об этом в лог один раз, а не молчит.
- **Между переключением и возвратом нет ни одного `yield`** — иначе чужой кадр застал бы
  сервер говорящим не на том языке. Возврат стоит в `finally`.
- **Русская колонка у выделенного сервера есть** — проверено в его собственном
  `valheim_server_Data/resources.assets`: 42 языка в шапке, `Russian` седьмой,
  `item_deerhide` → «Шкура оленя», `item_wood` → «Древесина». Если её однажды не
  окажется, `SetupLanguage` вернёт `false`, мод сразу вернёт прежний язык и напишет об
  этом; имена останутся английскими, но сервер не останется без слов.
- **Каталог шлётся раз за запуск**, так что имена на сайте меняются не в момент выкладки
  DLL, а при следующем старте игрового сервера. В логе это видно строкой
  `Sorting: told the site about N items (Russian names)` — язык там не для красоты: иначе
  каталог, уехавший не на том языке, заметен только на самом сайте и через сутки.
- **Проверено живьём на отладочном сервере 19.09.2026**: каталог доехал до сайта в 17:48,
  962 предмета, `$item_deerhide` → «Шкура оленя», `$item_wood` → «Древесина»,
  `$item_tar` → «Деготь». **Но самого переключения там не было и быть не могло**:
  Windows-сервер делит настройки игры с клиентом (один ключ реестра
  `HKCU\Software\IronGate\Valheim`, у обоих `app.info` — `IronGate/Valheim`), а у хозяина
  там русский. В логе видно, как игра делает это сама на старте: 17:39:41 грузит
  английский, 17:39:42 — русский, и больше загрузок языка за сессию нет. Поэтому
  `previous` совпал с русским, мод честно ничего не переключил, и **беда эта — только
  линуксовая**: на VPS настройки свои, там английский.
- **Проверить его на этой машине можно только сменив язык игры хозяину** — настройки
  общие, — так что не надо.
- **На VPS переключение отработало, 19.09.2026 в 14:59 UTC.** В логе игры три захода
  подряд по тринадцать файлов локализации: английский (это игра на старте), русский (это
  мод) и снова английский (это возврат). Ни одного предупреждения от мода, каталог ушёл
  строкой `told the site about 1097 items (Russian names, 417 skipped as not items)`, и на
  боевом сайте `$item_deerhide` — «Шкура оленя», `$item_tar` — «Деготь». То есть язык
  сервер отдаёт обратно, а не остаётся на русском до перезапуска.

**В каталог идут только настоящие предметы** (19.09.2026, 1.2.4). В `ObjectDB` рядом с
предметами лежат атаки существ — у `WolfAttack1`, `claw`, `jaws`, `Unarmed`, `spawn` тоже
есть `ItemDrop`, — и они ехали на сайт наравне со всем остальным. Среди английских имён
это терялось, а после перевода стало видно: два десятка строк, которые админ читает и не
понимает. Признак простой: **имя без `$` — не ключ перевода, а внутреннее имя**. Таких
префабов 417 из 1514, но имён у них на всех двадцать, так что на сайте это было 20 строк
из 982; осталось 962. Отброшенное считается и попадает в ту же строку лога — фильтр,
съевший лишнего, иначе не отличить от игры, потерявшей предметы. **Сайт строки не
удаляет** (каталог только дописывает и правит), поэтому те двадцать снесены руками:
`delete from game_sort_items where kind not like '$%' and category is null` — и на боевом
тоже, сразу после выкладки 1.2.4, иначе старый мод вернул бы их следующим стартом.
## Незакрытое (проверено 09.09.2026)

**Портал переехал на панель хозяина 12.09.2026 и выкачен на боевой.** Что сделано и
чего ещё нет:

- Перенесены и работают: вход через Steam, вайтлист с выгрузкой `permittedlist.txt` и
  `adminlist.txt`, статус серверов (A2S и чтение лога), суперадмин по
  `SUPERADMIN_STEAM_ID`, экраны в админке и кабинете. Проверено запросами и в браузере
  локально, а на боевом — `/api/health`, публичный список серверов и заведение
  суперадмина при старте.
- **На боевом живым человеком ничего не нажималось.** Вход через Steam там ещё не
  проходили ни разу, заявку не подавали, выгрузку списков не скачивали.
- **Экраны «Вайтлист» и «Серверы» причёсаны под телефон 12.09.2026** и выкачены:
  строки-карточки с тапом в модалку, поиск по нику и номеру, форма над таблицей в
  столбец, диалоги панели вместо нативных, тост не ложится на кнопки нижнего листа.
  Осталось: формы редактирования сервера нет вовсе (адрес и способ опроса правятся
  только удалением и добавлением заново), и на ширине 641–768 таблицы идут настоящие,
  шестиколоночные — запас там маленький, первый длинный ник включит прокрутку вбок.
- **Раздел «Наш сервер» на главной** (12.09.2026): руны и наигранные часы игроков плюс
  витрина общих построек. Всё из файлов, которые мод пишет для себя, — сайт их только
  читает: `astvard-currency.txt` (переживает перезапуски сервера),
  `astvard-templates-shared/*.txt` и `astvard.servermod.cfg`; разбор — в
  `src/protocols/valheimMod.js`, маршрут `/api/public/game`, путь к папке в
  `VALHEIM_MOD_CONFIG`. Наружу идут только ники и числа: SteamID64 не отдаётся ни из
  рун, ни из заголовка `#from` у присланных игроками шаблонов (на это есть тест).
  Игрока, которого нет у нас на сайте, зовём «Гость» и имени его персонажа не
  показываем: ник на сайте человек дал нам сам, а имя в игре — нет. **Разбор обязан
  повторять правила мода буквально**, иначе сайт уверенно рассказывает то, чего в
  игре нет: ставка рун зажимается в 1..1440, как `Mathf.Clamp` в
  `Plugin.Currency.cs` (в файле она остаётся какой вписали, и `MinutesPerRune = 0`
  значит «руна в минуту», а не «руна в час»); «открыта игрокам» — это ровно
  `#players yes`, а не наличие строки; пустой заголовок поле не затирает. Не
  показываем: подпапку `deleted` (корзина мода), файлы без `#name` (иначе наружу
  уйдёт имя файла) и **всё с заголовком `#from` — это присланное игроком, чего
  админ ещё не разбирал**: имя и категорию там пишет игрок, а страница открыта
  всему интернету.
  **Часы — оценка, а не счётчик**: они считаются как `руны × MinutesPerRune + остаток`,
  а при смене ставки мод прошлые руны не пересчитывает, так что после смены цифра
  поедет. Точное время мод нигде не хранит.
- **Что из игры можно показать сверх этого — тоже без правки мода**: правила сервера
  целиком (`astvard.servermod.cfg`, 33 значения в 7 секциях), паспорт мира с сидом
  (`_main.N.fwl2`), лента событий мода (выдача рун, постройки игроков, дорожки) — она
  в `BepInEx/LogOutput.log`, и для неё нужен второй путь в `.env`: сайт знает только
  `VALHEIM_LOG_FILE` и `VALHEIM_MOD_CONFIG`. Мод трогать имеет смысл только ради того,
  чего в файлах нет: координат, точного состояния зон и хода укладки дорожки.
- Модули панели про электрику (сотрудники, инструмент, автопарк, заявления, учёт
  времени, каталог) убраны из меню, но их код, маршруты и таблицы на месте. Удалять —
  отдельным заходом, когда станет видно, что за ними ничего не тянется.
- **Панель переведена на Postgres 12.09.2026.** `db.js` стал прослойкой над `pg`:
  `?` превращается в `$n`, `INSERT OR IGNORE` — в `ON CONFLICT DO NOTHING`, `lastID` —
  в `RETURNING *`, а пул держится в одно соединение с очередью, потому что весь код
  панели писался под последовательный `sqlite3`. Схема ушла в `db/schema.sql`, миграции
  выкинуты. Проверено локально живыми запросами (вход, статьи, вайтлист и обе выгрузки,
  серверы, медиатека, логи, дашборд) и `npm test` — 31 из 31.
- Старый портал (`backend/`, `frontend/`, `db/`) снесён, но лежит в истории git: если
  понадобится что-то ещё оттуда — `git show <коммит до 12.09.2026>:backend/...`.
- Данные прежнего портала на проде не переносились: там был один пользователь (хозяин)
  и одна строка сервера, оба заведены заново. База `astvard` в `postgres-shared` не
  тронута и не удалена.


Каждый пункт ниже перепроверен по коду в этот день, а не переписан из памяти.
Если правишь — проверь заново и обнови дату; протухший список хуже пустого.

**Везде 1.0.15 с 18.09.2026** (сетевая версия 40): клиент, отладочный сервер и VPS.
Мод против неё собран и проверен — 37 из 37 целей рефлексии на месте, 256 тестов,
ноль предупреждений компилятора; на VPS поднялся без единой ошибки мода и Jötunn.
Тогда же выяснилось, что мод собирается и против 1.0.12 без единой правки: перед
выкладкой на отстающий сервер это проверяется так — скопировать его
`assembly_valheim.dll` и `assembly_utils.dll` к остальным локальным сборкам, прогнать
по ним проверятеля рефлексии и собрать мод с `-p:ValheimDir=<эта папка>`.
**Осторожно: такая сборка пишется в обычный `bin/Release`** и потом уезжает в игру,
если не пересобрать.

**Игра обновилась до 1.0.12 (сетевая версия 40) 11.09.2026**, клиент и сервер разом, мир
получил WorldVersion 41. Мод собран против неё без ошибок; все Harmony-патчи и скрытые
члены (`AccessTools`) сверены с декомпилятом 1.0.12 через `ilspycmd` — на месте, имена
параметров совпадают; RPC печи, плавильни, браги и улья те же. Изменилось: `SpawnItem`
у печи получил `slot, userPoint, cheated` — наш префикс берёт только `name`; у браги
появился `m_delayedTapItemCheated`, наш `DelayedTap` его обходит; в
`WearNTear.ShouldUpdate` отсрочка в 30 с осталась, но деталь со снегом (`m_addPreSnow`)
проверяется и в ней. Сервер на 1.0.12 поднялся с модом без единой его ошибки. В игре
после обновления не проверено ничего. Там же найдено и закрыто: с 10.09.2026 зоны
автоматики не подтыкали землю — `PokeLocalZone` возвращает `bool` и в `Action` не
привязывался, а тихая перегрузка отдавала null. Зон на сервере не было, вреда нет; теперь
при каждом старте в логе BepInEx строка `Kept zones: ZoneSystem.PokeLocalZone bound.`

**Мод портирован на 1.0 и в игре работает.** Проверено в игре 09.09.2026:
`astvardadmin`, панель, установка спавнера с проекцией, укладка дорожки, запись отката.
Ошибок от мода в логах клиента и сервера — ноль. Тогда это держалось на Jotunn 2.29.2,
собранной под 0.221: она грузилась и патчила 1.0, хотя релиза под неё не имела.

**С 12.09.2026 везде Jotunn 2.30.0** — первая сборка под 1.0 (вышла 09.09.2026, в
заметках сказано «updated the majority of systems for Valheim 1.0.7»). Разложена в три
места разом, мод пересобран против неё: ноль ошибок и предупреждений компилятора, 137
тестов геометрии зелёные, `Loading [Jotunn 2.30.0]` в логе BepInEx и на VPS, и на
Windows-сервере, `Kept zones` на месте, warning'ов от Jötunn и мода ни одного. Старая
2.29.2 лежит в `.backups/jotunn-2.29.2/` (клиент и сервер) и на VPS как
`/srv/valheim/Jotunn.dll.2.29.2.bak`.

**В игре на новой Jotunn мод проверен в тот же день**, хозяином на VPS: панель открылась,
шаблоны прочитались (`Templates: 12 in 6 categories`), «Дом(Модуль)» на 122 детали дал
проекцию целиком (`Ghost preview: 122/122 pieces`, 205 точек прилипания) и отменился по
Esc. Ставить постройку не пробовали, остальные инструменты — тоже. Единственное новое
предупреждение Jötunn — `Ambiguous asset name for path` на клиенте, десяток строк про
камни и пещерные детали: игра 1.0 держит два пути к одному имени ассета, Jötunn берёт
первый. Нас это не касается, пока мы не просим у него эти префабы по имени.

Ради этого всё и затевалось, и **это закрыто живым входом 12.09.2026**. Раньше на
**каждом входе игрока** сервер писал `MissingFieldException: Field not found:
.ZRoutedRpc.Everybody … literal field` из
`Jotunn.Managers.SynchronizationManager.SynchronizeInitialData` — в 1.0 `Everybody` стал
`const`, а Jötunn, собранный под старую игру, читал его инструкцией для статического
поля, и начальная синхронизация Jötunn падала вместе с конфигами `IsAdminOnly` и его
статусом админа (10.09.2026; мы ни тем ни другим не пользовались, вреда не было).
На 2.30.0 хозяин зашёл на VPS трижды подряд — в логе сервера на том же месте
`Sending initial data to peer #…` и ни одной ошибки, а клиент получил
`Received admin status from server: Admin`. То есть синхронизация Jötunn не просто
перестала падать, а работает: конфиги с `IsAdminOnly` и предметы через Jötunn теперь
можно заводить.

Что 2.30.0 ломает у других и не ломает у нас: категории деталей в меню строительства игра
переделала целиком, и у кастомных деталей Jötunn категории больше нет. Мы `CustomPiece` и
`PieceManager` не трогаем вовсе — в коде мода их нет ни разу.

**С 19.09.2026 везде Jotunn 2.30.1** (вышла 18.09.2026). Чинит она ровно то, что 2.30.0
не доделала под 1.0 и что нас не касается: категории кастомных деталей в новом меню
строительства (`PieceConfig.Usage`, `PieceTableConfig.GuessUsage`, старый API категорий
объявлен устаревшим) и регистрацию `TerrainOp` в `ObjectDB.m_terrainOps`. Ни того, ни
другого в моде нет — обновились ради того, чтобы три установки не расходились, а не ради
починки. Мод пересобран против неё: ноль предупреждений компилятора, 296 тестов зелёные,
`Loading [Jotunn 2.30.1]` и `Loading [AstvardServerMod 1.2.1]` в логе BepInEx и на
Windows-сервере, и на VPS, `Kept zones` на месте, ни одной жалобы от Jötunn и мода.
**Зависимость самой 2.30.1 — BepInEx `5.4.2333`**, то есть тот, что у нас и стоит;
обновлять его незачем, хотя на Thunderstore с 09.09.2026 лежит `5.4.2350`. Старая 2.30.0
лежит в `.backups/jotunn-2.30.0/` (клиент и сервер) и на VPS как
`/srv/valheim/Jotunn.dll.2.30.0.bak`. **В игре на ней не проверено ничего.**

`ServerDevcommands` 1.109 при этом убран из обеих установок: на 1.0 у него пуст
`Settings`, а патч висит на `FixedUpdate` — 26 815 исключений и 12 МБ лога за
полчаса. Копия в `.backups/`, конфиги с ручными привязками клавиш оставлены на месте.

- **VPS: хозяин открыл и сайт, и игровой сервер** (12.09.2026). Первый вход в Docker
  состоялся — три подряд, `Got connection SteamID 76561198425108760` и
  `Got handshake from client`, отказа по вайтлисту нет. На сайте ровно один
  пользователь, хозяин, и он пока
  `player`: суперадмина на проде нет, назначать `UPDATE` из `deploy/README.md`. Сервер в список серверов сайта не внесён — A2S его не видит
  (см. `-public 0` выше), и строка вечно показывала бы «offline».
- **Ник и роль берутся из базы на каждом запросе, а не из сессии.** В сессии лежит
  только то, что записали при входе, и до 12.09.2026 оттуда же бралось всё остальное:
  `createSession` клала в Map и в таблицу `sessions` тройку `{id, username, role}`, а
  запрос получал её как есть. Отсюда «на главной Викинг08760, а в кабинете Meliowar»:
  `/api/auth/me` отдавал снимок, сделанный до того, как из Steam пришёл ник, а кабинет
  читал строку из базы. Хуже было с ролью: разжалованный админ оставался админом ещё
  сутки. Теперь `getCurrentUser` (`src/session.js`) читает строку по `id` на каждом
  запросе и обновляет снимок; исчезнувшая строка гасит сессию, а молчащая база —
  нет, иначе сбой базы выкидывал бы всех. Проверено live 12.09.2026: переименование и
  понижение в базе видны той же кукой сразу — `/api/auth/me` и 403 на `/api/logs`.
- **Статус сервера и список игроков читаются из лога игры** (12.09.2026,
  `src/protocols/valheimLog.js`). Лог печатает не только `Connections N ZDOS:` раз в
  десять минут, но и каждый вход-выход в тот же миг: `Got connection SteamID <число>`
  и `Closing socket <число>`. Проход по ним по порядку даёт точный состав онлайна, а
  номер там голый, без `V_`, и ложится прямо на `users.steam_id` — так число
  превращается в человека, и мод для этого не нужен. Счётчик из сердцебиения остался
  запасным: он отвечает, только если в прочитанном куске не нашлось ни одного события.
  Оттуда же берутся номер сохранения (`=> Save number N`) и версия игры (у сборки под
  Linux она пишется как `l-1.0.12`). Файл читается целиком, а не хвостом: игра
  обрезает его при каждом старте, и после часов игры он всё ещё десятки килобайт, зато
  хвост отрезал бы как раз тех, кто играет дольше всех. Проверено на боевом логе VPS
  (5 входов, 5 выходов, ноль онлайна, сохранение 153) и на синтетике с недостающим
  выходом. **С живым игроком на странице ещё не смотрели** — когда кто-то зайдёт,
  проверить, что имя появилось на главной и в админке.
- Регистрация не приводит email к нижнему регистру (`toLowerCase` в
  `handlers/auth.js` и `users.js` нет вовсе) — отсюда два аккаунта на один адрес.
- **Вайтлист в игре работает — с одной оговоркой** (12.09.2026). На VPS в
  `permittedlist.txt` стоит одна строка `V_<SteamID>` хозяина, и он вошёл: рукопожатие
  прошло, отказа в логе нет. Значит форма `V_` и включение списка первой же строкой —
  верны. **Не проверено обратное**: как список отказывает тому, кого в нём нет. Пока
  этого не видели, «вайтлист закрывает сервер» остаётся рассуждением, а не фактом, и
  проверять это надо чужим аккаунтом, а не рассуждением о коде. Сам файл собран руками,
  не выгрузкой из портала; по HTTP выгрузка проверена 11.09.2026 (заявка, решение,
  `V_<id>`, отказ при нуле одобренных, разметка в причине отказа не исполняется).
- **`adminlist.txt` в игре наполовину проверен** (12.09.2026). Форма `V_` принята: на
  входе мод написал `Admin granted to <peer>`, а клиент получил от Jötunn
  `Received admin status from server: Admin` — оба пути идут через `ListContainsId`.
  Форма `Steam_` по-прежнему держится только на разборе `ZNet.PlayerIsAdmin`. Файл при
  этом лежит на сервере с обеими формами, но положен руками, не выгрузкой из портала.
  Проверять оставшееся по кнопке кика
  в игровом списке игроков: она висит именно на `PlayerIsAdmin`, и если работает,
  значит форма `Steam_…` принята. Выгрузка и обе формы проверены по HTTP 11.09.2026.
- **Вход через Steam живым аккаунтом прошёл 12.09.2026** на проде: хозяин вошёл,
  завелась запись `Meliowar` с его `steam_id`, кука выдана, кабинет открылся. До этого
  были проверены только отказы — подделанная подпись, неподписанный `claimed_id`,
  чужой `return_to`, отмена у Steam, ответ без параметров (11.09.2026, обратный запрос
  к настоящему Steam). Первый же живой вход вскрыл то, чего отказы поймать не могли:
  аккаунт создавался с `steam_id_verified = false`, и кабинет встречал вошедшего
  строкой «вписан админом, войди через Steam чтобы подтвердить» — хотя Steam за номер
  только что расписался. Аккаунт из `src/routes/steam.js` заводится с
  `steam_id_verified = 1`; у ручного ввода админом флаг по-прежнему `0`.
  заведённой до правки, флаг чинится следующим входом (`markSteamIdVerified`).
- Ники из Steam не проходят `NICKNAME_RE` — намеренно, в Steam имя бывает любым, —
  так что через Steam «Пётр» заведётся, а через форму регистрации тот же ник
  получит 400 (см. пункт про «ё» ниже). Несогласованность известная.
- Автосинхронизации `permittedlist.txt` нет — файл переносится руками. Когда
  надоест: тянуть с сайта скриптом с машины сервера, по токену.
- Никнеймы с «ё» не регистрируются. В `NICKNAME_RE` (`handlers/auth.js`) стоит
  диапазон `а-я`, а `ё` — U+0451, вне него; `Ё` так же вне `А-Я`. «Пётр», «Алёна»,
  «Бьёрн» получают 400 с текстом про «буквы». Проверено живым запросом 11.09.2026.
- `Plugin.Automation.cs` — `MayHarvest` остаётся проверкой расстояния: сундук в зоне, но
  полный или занятый, всё равно кончится сбросом на землю. Чтобы закрыть, продукт надо
  протащить в каждую ветку.
- **Станции больше не угадывают сундук** (18.09.2026). Под назначенными сундуками жил
  второй, запасной проход: при включённых «Сбор в сундук» и «Наполнение» станция брала
  **любой** сундук в 8 м. Пометок сортировщика он не читал — ключи разные
  (`astvard_collect`/`astvard_supply` против `astvard_sort`), — и печь спокойно черпала
  руду из сундука «Материалы» и сыпала уголь в чужой. Хуже: слово «личный» для него
  ничего не значило, то есть сказать «не трогай» было нечем. Прохода больше нет:
  станция работает только с тем, на что показали. Личный сундук от этого защищён сам
  собой, **а назначенный личный работает** — назначение и есть разрешение, хозяин так и
  попросил. Оба переключателя остались, но значат теперь другое: не «плюс запасной
  режим», а «работает ли эта половина вообще». Поэтому у них **новые ключи** —
  `[Наполнение и сбор] Collect` и `Fill`, по умолчанию `true`: прочитать старое
  `AutoCollect = false` по новому смыслу значило бы молча выключить автоматику тому, у
  кого она работала. Старые строки в конфигах остаются лежать без дела.
  **В игре не проверено.**
- Общая библиотека шаблонов (`Plugin.SharedTemplates.cs`) ни разу не проверялась в игре.
- «Выйти из админки» (`LeaveAdmin`) — кнопка на главной панели под «Админ-меню»:
  снимает админ-режим без перезапуска игры, выключает бессмертие, полёт, бесплатную стройку
  и режим отладки, сбрасывает погоду и ветер, гасит проекции в руках. Написана 13.09.2026 и
  осталась несохранённой в основной папке, пока работа шла в worktree, — до игры не доехала;
  14.09.2026 перенесена в ветку дословно. **В игре не проверена ничем**: ни сама кнопка, ни
  возврат через «Админку», ни то, что взятая под админкой копия после выхода действительно
  пропала из рук, ни «Шаблоны сервера» после выхода — там должен остаться вид игрока.
- **Версия мода стала что-то значить** (16.09.2026, `1.1.0`). Живёт одной константой в
  `Plugin.cs`, сервер сообщает её в ответ на запрос панели, клиент со старой один раз за
  подключение пишет в чат «обнови». Никого не отключает: атрибута `NetworkCompatibility`
  нет намеренно. Пакет для Thunderstore собирается `tools/thunderstore/build.py`.
  **Номер обязан подниматься вместе с кодом.** 18.09.2026 этого не сделали, и наружу двое суток
  уходила сборка от 16-го: в релизе 1.1.0 и в живой сборке 1.1.0, а между ними шесть коммитов —
  предупреждение молчало, потому что сравнивает оно строку, а не содержимое. Порядок теперь один:
  поднять `Version` и `manifest.json`, собрать, разложить в три места, только потом релиз.
  **В игре не проверено ничем**: ни строка в чате, ни её отсутствие при совпадающих версиях, ни
  установка пакета менеджером. На Thunderstore последняя залитая версия — 1.2.1, и с неё
  листинг сообщества в отказе (`rejected`), то есть мода там не видно ни в поиске, ни по
  ссылке на страницу; остальные пакеты отданы хозяину файлами — заливка под его аккаунтом.
  **И то же самое повторилось 19.09.2026, только тише.** Релиз 1.2.0 вышел 18-го, после него
  в мод уехали шесть коммитов (общие зоны, раскладка с сайта, полка для добычи, повозка), и
  наружу всё это время уходила сборка до них. Поймалось не предупреждением, а тем, что хеш
  живой DLL на VPS не сошёлся с записанным в этих заметках. Вывод прежний и теперь стоит
  дважды: номер поднимать в том же коммите, что и код, а не в день релиза.
- Снос целых валунов дорожкой проверен в игре 10.09.2026: лог показал `Rock_3` и
  `Rock_4` среди снесённого и «left in the way: nothing». Лишних компиляторов рельефа
  за ту сессию ни одного, но сам отказ (`Terrain work turned down`) ещё ни разу не
  срабатывал. Дорожка на 172 м у спавна, проложенная раньше, по логу потеряла куски
  в двух зонах.
- Факелы вдоль дорожки проверены в игре 10.09.2026 админом: высота опоры деревянного
  факела измерилась 0.652 м — ровно как по чертежам игрока, — на дорожке 125 м встало
  52 из 52, откат снял все. Оплата из сумки у неадмина ещё не проверялась.
- Забор кольцом (`Plugin.Fence.cs`) проверен в игре 10.09.2026: кольца на 4, 10 и 19 м
  встали целиком, «Убрать последний забор» их снимал, опора кола 0.167 м над основанием.
  Меню дорожки по кнопкам тоже в деле: факелы выбирались через свою страницу. С помостом
  и крышей кольцо на 20 м встало целиком, 352 детали, и снялось 352 из 352 — после того
  как места стали судиться до постройки. Проекция по «Поставить» и выравнивание тоже в
  деле: кольца на 20 и 21 м с выравниванием встали целиком и сняты целиком. Закрепление
  проекции на P со сдвигом стрелками — у забора, шаблонов, спавнеров, моста и дорожки —
  и Esc без меню паузы в игре ещё не проверялись.
- «Заполнить» → «Пол» (`Plugin.AreaFill.cs`) в игре не запускался ни разу: ни прилипание
  плиты к стене, ни поиск контура по физике, ни красная линия через дыру. Проверены только
  заливка и раскладка плит в `Geometry` тестами. Прочность поднятого пола не считается:
  на балках без опор середина большого зала может провиснуть. Следом по очереди, как
  попросил хозяин: потолок, земля внутри, двускатная крыша.
- **Ставка ресурсов задаётся модом** (13.09.2026, `Plugin.WorldRates.cs`). Игра хранит её
  ключом мира `resourcerate <проценты>` (`Game.UpdateWorldRates` делит на 100) — это
  просто число, «×3 максимум» есть только в меню создания мира, а `-modifier` в строке
  запуска принимает лишь его варианты. Мод держит `[Мир] ResourceRatePercent` в конфиге
  и через несколько секунд после старта зоны кладёт ключ в мир, а раз в полминуты
  возвращает, если его сняли. При 100 ключ снимается, а не ставится в 100 — мир
  остаётся ванильным. Админ меняет ставку в «Настройках» → «Ресурсы: ×N»; клиент
  читает её прямо из синхронизированного ключа. Масштабируется дроп с деревьев,
  камней и руды (`DropTable`), собираемое (`Pickable`), мёд, смола и дроп с мобов, с
  потолком в размер стака; плавильня и брага сами не ускоряются. **Значение не из
  меню делает мир «с читами»** (`WorldContainsCheatedModifiers`) — игрокам на нём не
  засчитываются достижения Steam. На VPS стоит 500% с 13.09.2026: в логе BepInEx
  `Resource rate: 500% (was the game's own)`. Сам дроп в игре ещё не видели — смотреть,
  сколько дерева падает с одного бревна.
- **Сервер сам забирает списки доступа с сайта** (13.09.2026, `Plugin.SiteLists.cs`,
  `src/routes/gameSync.js`). Раз в минуту (`[Сайт] ListsSeconds`) мод идёт на
  `[Сайт] ListsUrl` с `Authorization: Bearer <ListsToken>` через `UnityWebRequest`
  корутиной — медленный сайт не держит кадр. Ответ — две секции `[permitted]` и
  `[admins]`, строки ровно в виде, который читает игра. Файл переписывается только
  если записи изменились, через временный файл и `File.Replace` (свежий mtime для
  `SyncedList.Load`); пустой `permittedlist.txt` не пишется никогда; сбой сайта
  пишется в лог один раз. Это вторая дорога к тем же файлам, которые сайт уже пишет
  сам, стоя на той же машине, — нужна, когда сервер живёт не здесь или запись
  сорвалась. Контейнер до `https://astvard.online` достаёт (проверено: 200,
  сертификаты в образе есть). Включено на VPS 13.09.2026: в журнале nginx запросы мода
  раз в минуту с кодом 200, в логе мода ни одной жалобы. Файлы совпадали с сайтом,
  поэтому мод их пока ни разу не переписал — запись проверится первой же выдачей
  доступа, когда сайт по какой-то причине сам не запишет.
- **Сундуки переносятся в шаблоне вместе с содержимым** (12.09.2026, `Plugin.Blueprints.cs`).
  Содержимое берётся байтами из ZDO сундука (`ZDOVars.s_items` — туда пишет сам
  `Container.Save`, проверено декомпиляцией 1.0.12) и едет девятым полем строки детали в
  base64; строка из восьми полей — деталь как раньше, старые шаблоны читаются. При
  постановке байты кладутся в ZDO нового сундука и сразу в его инвентарь. Потолок —
  8 КБ base64 на сундук, больше — сундук копируется пустым с предупреждением в логе.
  **Содержимое снимает только админ** (у игрока копия с добром — печать ресурсов), и
  **сервер срезает девятое поле у любого шаблона, который отдаёт игроку**
  (`WithoutChestContents` в `Plugin.BuildPause.cs`). Клиент и сервер обязаны быть на
  одной сборке: старый клиент строку из девяти полей молча пропустит, и сундук из
  постройки пропадёт. Разложено и сервер перезапущен 12.09.2026, мод поднялся без
  ошибок; **в игре ещё не проверено** — ни снятие, ни постановка, ни срез для игрока.
- «Отменить постройку» (`Plugin.LastBuild.cs`) с 10.09.2026 заменила «Убрать последний
  забор» и «Убрать последнее заполнение»: одна кнопка на всех страницах «Построек» для
  последнего, что поставили шаблон, копия, спавнер, забор или пол. Готовую постройку
  снимает со второго нажатия, идущую останавливает с первого и разбирает поставленное;
  выравнивание под ней откатывается вместе, пока оно последнее в откате. Там же чертёж
  до 300 деталей стал вставать за один кадр, больше — по 10 за 0.4 с (было по 5). В игре
  не проверено ничего из этого: ни сколько длится кадр на 300 деталей, ни остановка на
  полпути, ни откат земли вместе с чертежом.
- **«Шаблоны» открываются выбором «Мои шаблоны» / «Шаблоны сервера»** (14.09.2026), у админа
  и у игрока одинаково, дальше — категории с числом построек и сами постройки, с теми же
  именами и категориями, что у выложившего; страницы «Общие» больше нет. Админ видит в
  «Шаблонах сервера» всё, что лежит на сервере (иначе негде открыть присланное игроком или
  удалить лишнее), игрок — только открытое ему, и переименовать его не может: только свои.
  Раньше общие у админа («Общие»), у игрока («Шаблоны сервера») и «Шаблоны игрокам» в
  настройках шли сплошным списком, и всё после восьмой кнопки было недостижимо: под списком стояло
  «Показаны первые 8». Теперь у каждого длинного списка под кнопками «Выше» и «Ниже» (на
  страницу), колесо мыши над панелью листает по строке, а последняя страница всегда полная
  (`MenuPaging.cs`, покрыт тестами). «Шаблоны игрокам» перечисляет открытое всем или
  выбранным на сайте, а не открытое самому админу — с доступом по имени это разошлось.
  **В игре не проверено ничего** из этого: ни раскладка, ни колесо, ни переходы «Назад».
- «Разрешить игрокам» / «Запретить игрокам» (`Plugin.PlayerBuilds.cs`) с 10.09.2026: у
  админа на странице шаблона и серверного шаблона, у игрока — пункт «Постройки» в корне
  панели, пока открыт хоть один. Разрешённый шаблон уходит на сервер и помечается строкой
  `#players yes` в своём файле в `astvard-templates-shared`; тело шаблона сервер отдаёт
  неадмину только с этой меткой. Игрокам бесплатно и без верстака — хозяин выбрал это
  сознательно, зная про печать ресурсов разбором молотом. Чужие обереги проверяются по
  каждой детали, как у молота. В игре не проверено ничего, и держится всё на общей
  библиотеке, которая сама в игре не проверялась. Смотреть игроком: разрешить, выйти и
  зайти заново, «Админку» не нажимать.
- Пауза между постройками игрока (`Plugin.BuildPause.cs`, по умолчанию 5 мин) считается
  на сервере по id сокета — ник и персонаж её не сбрасывают, перезапуск сервера сбрасывает.
  Шаблон игрок получает без счёта; счёт начинается по ЛКМ: призрак замирает, клиент
  спрашивает сервер и строит только по «да». Касается всех, кто строит через «Постройки»
  игрока, админа без «Админки» тоже. Меняется в «Админ-меню» → «Настройки», там же список
  открытого игрокам. Значение — `PlayerBuildPauseMinutes` в конфиге сервера. В игре не
  проверено.
- «Рельеф для игроков» (`Plugin.PlayerRules.cs`) с 11.09.2026: в «Настройках» у каждого
  инструмента свой переключатель, у выравнивания, дорожки, площадки и моста ещё лимит в
  метрах, отдельно — платные ли факелы. Значения — секция «Рельеф для игроков» в конфиге
  сервера; по умолчанию всё как было: открыто всё, кроме сноса, лимиты во всю длину
  инструмента, факелы платные. Снос, открытый игроку, не трогает то, что стоит в чужом
  обереге. Проверено 11.09.2026: лимит площадки 8 м дошёл до сервера (`AreaMax = 8`), и
  площадка игрока встала ровно 8 м — но 8 м ещё и значение поля по умолчанию, так что
  урезание этим не доказано. С тех пор урезанный радиус площадки и выравнивания игроку
  называется в углу экрана и пишется в лог: `radius N held to the players' limit L`.
- «Постройки для игроков» и «Функции для игроков» (`Plugin.PlayerRules.cs`) с 11.09.2026:
  прилипание; пол и забор — нет/даром/платно; копирование своих деталей в радиусе и
  сохранение в «Мои шаблоны», ставятся копии только платно; «Поделиться» — присланное
  ложится в «Общие» с именем игрока, до 5 штук и 3000 деталей на игрока; ремонт в 32 м —
  чинит даром, заправка нет/даром/платно; своя зона автоматики — одна на игрока, по id
  сокета, радиус до лимита. Пауза держит только бесплатное: шаблоны сервера, пол и
  забор даром. По умолчанию: прилипание да, пол, забор и ремонт платно, копирование да до
  20 м, делиться нет, зона нет. Страницы: «Настройки» → «Постройки для игроков» (там же
  пауза и открытые шаблоны), «Рельеф для игроков», «Функции для игроков». В игре не
  проверено ничего; платёж — `Bill` в `Plugin.Payment.cs`, `RemoveItem` в игре не звался.
- Руны (`Plugin.Currency.cs`) с 11.09.2026: внутриигровая валюта, +1 за каждые 60 минут
  в мире (`MinutesPerRune` в секции «Руны» конфига сервера; админ меняет в «Настройках» → «Руна за …», действует сразу). Считает сервер раз в 10 с по
  подключённым игрокам с персонажем, по id сокета, остаток часа переносится на следующий
  заход. Хранится в `BepInEx\config\astvard-currency.txt` на сервере, пишется раз в минуту
  и на выходе. Игрок видит баланс вверху первой страницы панели и под панелью быстрых
  предметов (дочерний объект `HotkeyBar`, спрашивается с появлением персонажа, не с первым
  открытием инвентаря). Проверено в игре 11.09.2026 на `MinutesPerRune = 1`: 8 рун за
  8.5 минуты, файл записался; после проверки вернули 60, а тестовый файл отложен как
  `astvard-currency.test-20260911.txt`. Надпись под панелью в игре ещё не видели. Тратить
  руны пока не на что.
- **Ведомость рун и выдача** (18.09.2026, `Roster.cs`, `Plugin.RunePage.cs`, серверная
  половина — в `Plugin.Currency.cs`). Один список и на игру, и на сайт, склеенный по
  SteamID64: у сервера балансы и имя персонажа, у сайта ники и все, кто вообще
  зарегистрирован. Ключ общий сам собой — это то же число, что в файле кошельков и в
  `users.steam_id`. Правила склейки, поиска (с «ё» = «е») и границы выдачи вынесены в
  `Roster.cs` — без Unity, 13 тестов; **сайт обязан повторить их на своём языке**, позвать
  этот файл он не может, как и с разбором рун. Ведомость (`AstvardRoster`) уходит только
  админу и только тому, кто спросил: в ней платформенные id всех игроков, а панель висит у
  всех. Выдача (`AstvardRuneGrant`) пишет кошельки на диск сразу, отрицательное число
  забирает, ниже нуля не уходит, за одно нажатие не больше 10 000. В панели —
  «Админ-меню» → «Руны игроков»: поиск по нику, имени персонажа и номеру, список страницами,
  строка открывает игрока. **Проверено:** сборка без предупреждений, 198 тестов, загрузка на
  отладочном сервере (`Loading [AstvardServerMod 1.1.0]`, ни одной ошибки мода и Jötunn),
  новая настройка записалась в конфиг. **Не проверено ничем:** вся страница в игре, сама
  выдача, сообщение игроку, список длиннее восьми строк. На VPS эта сборка не выкладывалась —
  режим разработки; там прежняя.
- **Счётчик рун стоит у неподвижного** (18.09.2026): `IdleMinutes` в секции «Руны» конфига
  сервера, по умолчанию 20 минут, 0 — не проверять вовсе. Два признака активности, любого
  хватает: позиция, которую шлёт клиент, и ревизия ZDO персонажа — почему это ловит крафт у
  верстака, написано в `valheim-mod/CLAUDE.md`. Баланс и начатый час остаются, никого не
  выгоняет. Новое подключение считается действием: кошелёк живёт в памяти и после выхода, и
  вернувшийся на то же место иначе был бы признан спящим с первой же секунды. **В игре не
  проверено**; сработало ли — по строке `has done nothing for N min` в логе BepInEx.
- **Сайт про руны пока не знает ничего** (18.09.2026): ни страницы «Руны» в админке, ни
  канала выдачи с сайта, ни ников, уходящих серверу (`SetRosterAccounts` есть и никем не
  зовётся). Упёрлось в стенд: Postgres на этой машине живёт только в Docker, а служба
  `com.docker.service` остановлена и не стартует без прав администратора. Писать админку
  вслепую здесь уже пробовали — вышла страница настроек, падавшая на пустом месте.
- **Сортировщик** (18.09.2026, `Sorting.cs`, `Plugin.Sorting.cs`, `Plugin.SortPage.cs`).
  Отдельная от зоны автоматики вещь и нарочно клиентская: разбирает непомеченные сундуки и
  тележки в помеченные, **пока игрок стоит в своей зоне**. Зона ставится проекцией по земле,
  как площадка мощения, — круг или квадрат на выбор до установки, ЛКМ ставит, Esc отменяет,
  P закрепляет, стрелки двигают; зон может быть несколько, они перечислены на своей странице
  и **не должны пересекаться** (иначе у одного сундука два хозяина). Пометка живёт в ZDO
  самого сундука (`astvard_sort`, `astvard_private`), переживает перезапуск и видна в
  подсказке при наведении. «Личный» — ни источник, ни приёмник. Тележка — это `Container` в
  `Vagon.m_container`, своего случая ей не нужно. Порядок выбора: сундук, где **уже лежит
  такое же**, потом категория сундука, потом «Разное»; полный пропускается на каждом шаге, на
  землю не бросается ничего. Перекладывание идёт только публичными `GetInventory`,
  `CanAddItem`, `RemoveItem`, `AddItem` — ни одной RPC и ни одной строки рефлексии, то есть
  мимо той ямы, что дважды убивала автоматику. Правило для игроков — «Сортировка» в
  «Функциях». **Проверено:** сборка без предупреждений, 216 тестов, 37 целей рефлексии,
  загрузка на отладочном сервере, запись настроек (`[Сортировка] Enabled`, `Zones`, правило
  `Sorting`). **Не проверено ничем в игре:** ни проекция зоны, ни квадрат, ни пометка
  сундука, ни сам разбор, ни то, **находит ли обход тележку** — она попадает в сортировку
  только если у неё есть `Piece`, а это предположение, а не факт.
- **Клетки зоны автоматики видно до установки** (18.09.2026, `Plugin.ZonePreview.cs`).
  Кнопка «Показать клетки» на обеих страницах зон — у админа в «Зоне автоматики», у игрока
  в «Моей зоне». По земле рисуется **сетка тех самых клеток по 64 м**, которые зона будет
  держать загруженными, и поверх неё бледным — круг запрошенного радиуса: видно, что просил
  и что получишь. Ставится по ЛКМ той же RPC, что и прежние кнопки «Добавить здесь» /
  «Поставить здесь», — они остались. Esc отменяет, P закрепляет, стрелки двигают. Клетки
  считает `Plugin.Zones.CellsFor` — **та же функция, которой зона живёт на сервере**, а не
  вторая копия арифметики рядом; сетка перерисовывается, только когда клетки меняются, то
  есть при переходе через край клетки, а не каждый кадр. Игроку радиус урезается до его
  лимита прямо в показе. **В игре не проверено ничем.**
- **Зона сортировки вращается Q и E** (18.09.2026): квадрат, на 22.5° за нажатие — сетка
  самой игры, 16 щелчков на круг, — с Shift вчетверо мельче. Круг на Q/E отвечает словами,
  что вращать его незачем. Угол хранится в зоне и **учитывается проверкой «внутри ли
  сундук» и проверкой пересечения зон** (разделяющие оси, `BoxesMeet` в `Sorting.cs`), а не
  только рисунком: зона, которая выглядит повёрнутой и сортирует прямо, хуже неповорачиваемой.
  17 тестов на это, включая случай, где два квадрата по 10 м в 24 м друг от друга не
  пересекаются прямыми и пересекаются, когда один повёрнут на 45°. Записи зон, сделанные до
  угла, читаются как неповёрнутые. **У зоны автоматики вращения нет и быть не может** —
  причина там же, где и с формой: единица там целая клетка 64 м.
- **Что включено, видно под панелью быстрых предметов** (18.09.2026, `Plugin.StateHud.cs`):
  строка «Включено: админка · бессмертие · полёт · отладка · стройка даром», и её нет вовсе,
  когда ничего не включено. Всё читается публичными `InGodMode`, `IsDebugFlying`,
  `Player.m_debugMode`, `NoCostCheat` — компилятор присмотрит. Обновляется 4 раза в секунду.
  Полёт особенно ради этого: кнопки у него в панели нет вовсе, он включается Z в отладке, и
  замечают его обычно на земле. На кнопках «God» и «Debugmode» теперь тоже написано «вкл» /
  «выкл». **В игре не проверено.**
- **Исправлено там же**: проекции зоны сортировки и клеток зоны автоматики не попадали в
  `PlacementHoldsInput`, то есть ЛКМ, ставивший зону, заодно бил топором, а Q (у игры это
  автобег) отправлял персонажа трусцой. Обе теперь в списке.
- **Внутри категории сундуки делятся сами** (18.09.2026, `Sorting.MakePlan`). Пометил три
  сундука «Материалы» — мод сам решит, что куда: куча, занимающая `OwnChestSlots` ячеек и
  больше (в конфиге, по умолчанию 4), получает **свой сундук целиком**, и туда не кладут
  ничего другого — пустые ячейки ждут следующей поставки; всё, чего по одному или по десятку,
  ложится вместе в общий. Мерится в **ячейках, а не в штуках**: 500 дерева по 50 в стопке —
  это 10 ячеек, а 30 медной руды — одна, и в этом вся разница. Куча, которой мало одного
  сундука, берёт несколько; что не влезло, идёт в общий, а не остаётся в тележке.
  **Один сундук категории не занимается никогда** — иначе категории из одного сундука было
  бы некуда класть мелочь, а сваливать её к дереву — ровно то, чего мы избегаем.
  **Раскладка нигде не хранится и считается заново каждый проход**, отсюда всё остальное
  задаром: куча, переросшая порог, получает сундук сама; истраченная — сундук отдаёт, и он
  возвращается в общий котёл; что лежит не там, переносится (`TidyBins`), иначе дерево так и
  осталось бы в общем сундуке, а свой стоял бы рядом пустым. Чтобы решение не скакало,
  сундук под кучу выбирается по тому, **чего в нём уже больше** — перенос туда же это только
  усиливает, — а равные разрешаются порядком сундуков **по координатам**, а не по тому, в
  каком порядке их вернул обход. `OwnChestSlots = 0` возвращает прежнее поведение. 16 тестов,
  включая пример хозяина: 500 дерева, 200 камня, 300 слитков, 30 меди, 2 олова и четыре
  сундука — три кучи разъезжаются по своим, руда ложится в четвёртый. **В игре не проверено.**
- **Куча держится одного места** (18.09.2026, `Sorting.Groups`, `PickGroup`). Раскладка
  знала, что в сундуке лежит, и ничего — где он стоит: два сундука категории были
  взаимозаменяемы, и дерево, которому не хватило одного, могло получить второй в другом
  конце зоны. Теперь у сундука в `BinState` есть координаты, и из них две привычки.
  **Первая:** после первого сундука куча берёт **ближайший к нему**, а не следующий по
  списку. **Вторая:** сундуки, стоящие рядом, считаются кучкой (одиночная связь: сосед
  ближе `GroupSpan` — уже своя, поэтому стена сундуков остаётся одной кучкой любой
  длины, а соседняя комната — отдельной), и куча берёт сундуки **из одной кучки** — той,
  где её уже больше, а при равенстве большей. Не хватило места в своей кучке — остаток
  идёт в общий сундук, **а не через двор**: половина дров в подвале хуже, чем дрова и
  немного дров в миксе. Настройка — `[Сортировка] GroupSpan`, 8 м по умолчанию, 0 —
  не собирать в кучки вовсе (прежнее поведение, на нём же стоят старые тесты). Раскладка
  по-прежнему **нигде не хранится** и считается заново каждый проход, так что разъехавшаяся
  куча сама стягивается обратно переносами. Скачков от прохода к проходу нет: кучка, куда
  добро уже поехало, с каждым переносом держит его больше и выигрывает тем же правилом.
  6 тестов, всего 262. **В игре не проверено.**
- **Зона сортировки стала общей** (18.09.2026, `Plugin.SharedZones.cs`, `Plugin.SortCrew.cs`).
  Зона жила в конфиге того, кто её нарисовал, и всё, что из неё следует, — подписи над
  сундуками и сам разбор — жило там же. На сервере, где базу держат вдвоём, хозяин у неё
  оказывался не тот: пригнанная повозка стоит полной, потому что человек, в чьём конфиге
  лежит зона, сегодня не зашёл. Теперь **зоны хранит сервер**, файлом
  `BepInEx/config/astvard-zones.txt`, по зоне на строку; у каждой есть хозяин и вписанные,
  по SteamID64. Пометки сундуков были общими с самого начала — они лежат в самих
  сундуках, — так что это был последний кусок, который не был.
  - **Раздаёт сервер, раз в секунду и только изменившееся**: каждому игроку — те зоны, где
    он хозяин или вписан, и сравнение с тем, что этому же сокету отправляли прошлый раз.
    База, где ничего не происходит, стоит одного сравнения строк в секунду.
  - **Разбирает в зоне только один клиент**, и кто именно — решает сервер: из тех, кто
    внутри (по `m_refPos`, той позиции, которую шлёт сам клиент), с наименьшим номером.
    Двое, тянущие одну стопку из одной повозки, — это предмет из ниоткуда или предмет в
    никуда, и ни строчки об этом нигде. Остальные в это время видят подписи, а страница
    честно пишет «в этой зоне сейчас разбирает другой игрок».
  - **Вписывают по нику и только тех, кто в игре.** Номер Steam наружу не уходит вовсе:
    клиент шлёт ник, сервер сам ищет его среди своих сокетов. Имена для показа сервер
    прикладывает отдельной строкой из кошельков — там имя есть у всякого, кто здесь играл,
    поэтому вписанный полгода назад читается человеком, а не числом.
  - **Вписанный работает в зоне, но не трогает её**: ни размер, ни форму, ни перенос, ни
    удаление — `Sorting.MayEdit` отвечает «да» только хозяину и админу. Право вписывать
    закрывается правилом «Общие зоны» в «Функциях для игроков» (по умолчанию открыто).
  - **Пересечения проверяет сервер и по всем зонам сразу.** Клиент видит только свои, то
    есть на вопрос «не занято ли здесь» ответить не может: сундук в двух зонах — это два
    хозяина, и чьи они, ему безразлично.
  - **Страница держится за номер зоны, а не за место в списке** (`FollowEditedZone`):
    список приходит заново при каждой чужой правке, и страница, помнящая место, ответила
    бы «Удалить» про ту зону, которая туда переехала.
  - **Старые зоны уезжают на сервер один раз** — при первом входе, если у сервера моих
    ещё нет; `[Сортировка] ZonesSent` это отмечает, иначе удалённая нарочно зона
    возвращалась бы каждый вход. Одиночная игра и сервер без мода ничего этого не
    замечают: пока сервер ни разу не ответил, всё работает по-старому, из конфига.
  - Формат строки зоны дорос до десяти полей (`id`, `owner`, `вписанные`, `за рулём`), и
    запись из шести читается как прежде — зона, которая никогда не была ничьей, кроме
    своего клиента. 14 тестов, всего 276; 37 из 37 целей рефлексии.
  **В игре не проверено ничем**: ни раздача, ни вписывание, ни то, что за рулём
  оказывается ровно один. Смотреть вдвоём и по строкам `Sorting zone` в логе клиента.
- **Подписи над помеченными сундуками и кнопка помощника** (18.09.2026,
  `Plugin.ChestLabels.cs`, страница опыта в `Plugin.HelperTest.cs`). Пометка сундука была
  видна только при наведении, а смысл разметки стены сундуков в том, чтобы охватить её
  взглядом; сам Valheim сундуки переименовывать не умеет — ванильный способ поставить рядом
  табличку, — поэтому слово над сундуком рисует мод. `TextMesh` шрифтом Jötunn, пул до 40
  штук, только внутри зоны, где стоишь, и только в 40 м; поворот к камере каждый кадр, обход
  сундуков — дважды в секунду, и `.text` присваивается только при изменении, иначе Unity
  пересобирает меш надписи на каждом проходе. Переключатель «Подписи: вкл/выкл» на странице
  «Сортировка». Помощник из опыта теперь ставится кнопкой «Функции» → «Помощник (опыт)», а не
  командой в консоли, и только у админа. **В игре не проверено ни то, ни другое** — особенно
  подписи: не видел ни размера, ни того, читаются ли они на фоне построек.
- **Подписей над сундуками не видно, и мод об этом молчал** (18.09.2026). Лог клиента
  при этом честно писал `Sorting zone: bins 5, sources 17` — то есть игрок стоит в зоне,
  а мод видит пять помеченных сундуков; подписи ищут те же сундуки тем же кодом и той же
  проверкой `Sorting.Inside`, так что дело не в зоне и не в пометках. Причина **не
  установлена**, и установить её было нечем: `MakeChestLabel` возвращал `null`, если
  Jötunn не отдал шрифт, обход молча обрывался на первом же сундуке — ни ошибки, ни
  строки в логе. Теперь шрифт ищется в четыре захода (`AveriaSerifBold`, `AveriaSerif`,
  любой уже загруженный — динамический вперёд, — шрифт системы) и **один раз называется
  в логе**: `Label font: <имя> (dynamic|baked)`. Запечённому шрифту размер не задаётся:
  у такого шрифта он один, и просьба о другом собирает у `TextMesh` пустой меш — ещё
  один способ не нарисовать ничего молча. Сколько подписей поднято, говорит строка
  `Chest labels: N over marked chests` и сама кнопка: «Подписи: вкл (5)». Тем же
  `AveriaSerifBold` рисуются строка рун и строка «Включено:» под панелью быстрых
  предметов — **обе в игре тоже ни разу не были видны**, и обе теперь берут шрифт через
  тот же поиск. Текст самой панели оставлен на своём `AveriaSerif`: он виден, и делать
  его жирным никто не просил.
- **Дверг ходит за игроком — опыт с помощником удался** (18.09.2026, проверено в игре
  хозяином). Существо игры ставится, слушается `SetFollowTarget`, ходит между постройками
  само. Значит, для помощника не нужно ни своего существа, ни своего поиска пути — берётся
  готовое. Осталось непроверенным: как он выглядит вблизи, не дерётся ли, и что будет, когда
  ему дадут работу.
- **Повозка, пригнанная в зону, не разобралась** (18.09.2026, найдено в игре). Причин у
  этого полдюжины, и панель не называла ни одной, поэтому сперва добавлено объяснение, а не
  догадка: страница «Сортировка» теперь пишет, **почему** разбор не идёт — выключено, правила
  закрыты, сервер их ещё не прислал, зон нет, ты вне зоны, в зоне нет помеченных сундуков,
  или сколько всего нашлось. В лог клиента раз на изменение уходит строка
  `Sorting zone: bins N, sources M, carts K`, и `carts` отвечает прямо на главный вопрос:
  попадает ли тележка в обход по `Piece` вообще (`Piece.Awake` кладёт в список все детали без
  условий, так что если `carts 0` при тележке в зоне — у неё просто нет `Piece`, и искать её
  надо иначе). Причина пока **не установлена**.
- **«Назад» — первой строкой и на каждой странице** (18.09.2026). Кнопка одна на всю
  панель, поэтому наверх она поднимается порядком детей (`SetAsFirstSibling`), а не порядком
  создания: виджеты рождаются кучами по темам, и держать её первой среди них пришлось бы
  помнить в каждом новом файле. Условие показа стало `MenuState != StateRoot` — прежнее
  перечисляло «страницы игрока» списком, и стоило добавить «Сортировку», как неадмин
  оставался на ней без выхода. **Это второй раз**: тот же список однажды уже запирал
  админ-страницу с низким номером. Список (`IsPlayerSection`) убран совсем, чтобы не
  дождаться третьего.
- **Повозка нашлась, а разбор — нет: «Разное» перестало быть запасным вариантом**
  (18.09.2026, найдено хозяином в игре). Счётчик тележек ответил сразу: `Piece` у повозки
  есть, обход её видит, источником считает. Сломал это я сам, переписывая раскладку:
  `Mixed` стал строго по категориям, и предмет искал сундук только своей — то есть при одном
  помеченном «Разное» сундуке дерево не ехало никуда. Теперь «Разное» снова принимает всё,
  чему не нашлось сундука своей категории, но **после** своей категории, а не вместо неё;
  два теста. Там же страница стала говорить, почему предмет остался на месте: «некуда» и
  «полно» — разные беды и чинятся по-разному.
- **Сортировка отработала в игре** (18.09.2026, проверено хозяином): тестовый сундук,
  поставленный кнопкой «Постройки» → «Сундук для проверки», разобрался по помеченным
  сундукам. Вся цепочка живая — пометки, зона, раскладка, перенос. Оставшиеся в повозке
  предметы оказались не поломкой: страница честно написала «Не влезло: 7» — в помеченных
  сундуках не было ни свободной ячейки, ни начатой стопки того же. **Стопка переносится
  целиком**, поэтому даже пять ягод требуют либо своей ячейки, либо такой же стопки с
  местом; докладывать стопку по частям мод пока не умеет. Тогда же выяснилось, что
  `m_shared.m_name` — ключ перевода (`$item_bronzenails`), и показывать его игроку нельзя;
  в моде для этого уже был `ItemTitle`.
- **Сортировщик работает только внутри зоны** (18.09.2026, после двух заходов). Сперва у
  зоны была кайма 16 м, чтобы разбиралась повозка, брошенная у ворот, и отдельное
  правило «повозка в 10 м от игрока». Два расстояния сложились в 26 м от края, и
  повозка стала разбираться там, где зоной уже не пахнет — хозяин это заметил и решил:
  одно правило, что внутри зоны, то и разбирается. Зону проще расширить, чем помнить
  два складывающихся расстояния. Повозка теперь ничем не отличается от сундука, разве
  что помеченная повозка может стать приёмником — она ведь стоит в зоне.
- **«28 м» в списке зон — это расстояние до зоны, а не её радиус** (18.09.2026). Строка
  с именем показывала только имя и расстояние, размер из неё пропадал, и метры
  принимали за радиус: поставил 10, применил, а в списке всё те же 28. Теперь в строке
  и имя, и форма с размером, а расстояние подписано словами «до неё».
- **Из повозки ничего не переносилось: владелец сети у неё не там** (18.09.2026, найдено
  хозяином в игре, исправлено). У сундука `ZNetView` висит на том же объекте, что и
  `Container`, **а у повозки `Container` сидит на дочернем объекте, `ZNetView` — на корне**.
  Перенос брал `container.GetComponent<ZNetView>()`, получал у повозки `null` и выходил, не
  сдвинув ничего: повозка честно попадала в источники, считалась в логе строкой `carts N`,
  а добро не ехало — и всё это отчитывалось как «не влезло», потому что неудачный перенос
  ничем не отличался от занятого места. Теперь владелец ищется вверх по объекту
  (`GetComponentInParent`, помощник `ViewOf`), и через него же читаются и пишутся пометки —
  иначе повозку нельзя было бы ни пометить, ни объявить личной. Если владелец всё же не
  найдётся, мод один раз пишет об этом предупреждение, а не молчит. **То же место есть в
  `Plugin.Automation.cs`** (`target.GetComponent<ZNetView>()`): для сундуков оно верно, для
  повозки сломается так же — не трогал, но знать стоит.
- **Повозка разбирается — проверено в игре** (18.09.2026). Дело было именно в владельце
  сети: как только его стали искать вверх по объекту, добро поехало.
- **Страница зоны сортировки** (18.09.2026): клик по строке в списке больше не стирает
  зону — он её открывает. Там имя (живёт в той же строке конфига шестым полем, запятые и
  точки с запятой из него вычищаются, иначе файл разорвало бы и зоны пропали все разом),
  расстояние от середины с той же проверкой пересечения, что и при установке, подсветка
  границ по земле и удаление вторым нажатием, как у всего, чего не вернуть.
- **Сортировщик не трогает сундук подачи** (18.09.2026). Наполнение и сбор
  (`Plugin.Automation.cs`) с сортировкой **никак не связаны**: свой цикл, свои пометки в
  ZDO (`astvard_collect`, `astvard_supply`), свой радиус 24 м вокруг игрока, свои кнопки
  «Назначить сундук». Для сортировщика такой сундук выглядел непомеченным, то есть
  источником, и он вычерпал бы подачу в первую же минуту — печи встали бы, и ни одной
  ошибки в логе. Теперь сундук подачи пропускается совсем. Сундук **сбора** остаётся
  источником намеренно: туда падает готовое, и разложить его — ровно работа сортировщика.
- **Зона у наполнения и сбора, и форма у обеих зон** (18.09.2026). Даль, с которой станция
  видит назначенный сундук, была зашита числом 24 — теперь это настройка
  (`[Наполнение и сбор] ChestRadius`, 8–64 м) со своей страницей «Зона станций»
  (19.09.2026 переименована из «Зона сундуков» и уехала в «Работу с сундуками»):
  размер, форма круг/квадрат и подсветка. Круг рисуется **вокруг игрока, а не вокруг
  станции**: станций много, и обводить каждую значило бы закрыть базу кольцами, а даль у них
  одна и та же. Автоматика перестала мерить расстояние в трёх местах по-своему и спрашивает
  одну проверку `InChestZone`; заодно высота перестала считаться, как и у зоны сортировки —
  сундук этажом ниже стоит в том же доме. У зоны сортировки форму теперь можно менять и
  **после** установки, а саму зону — переносить: кнопка «Перенести» поднимает ту же проекцию,
  с теми же ЛКМ, Esc, P, стрелками и Q/E, только форма и размер берутся у неё самой. Любая из
  трёх правок — размер, форма, перенос — проходит через одну проверку пересечения
  (`ReplaceSortingZone`): ошибаются они одинаково, и стоит это одного сундука с двумя
  хозяевами. **В игре не проверено ничего из этого.**
- Длинная дорожка админа (`Plugin.RoadJob.cs`) с 11.09.2026: длиннее 100 м её кладёт
  сервер — клиент меняет только землю в 5x5 зонах вокруг себя, 130–190 м в каждую сторону,
  а сервер тыкает зоны вдоль пути сам, строит их объекты и кладёт по 100 м: сглаживание,
  снос, покраска и факелы тем же кодом, что у клиента, ход укладки — сообщениями. Предел
  10 км (было 300 м с закреплённым концом). Остановка — «Остановить укладку» на странице
  дорожки, не Esc: админ волен заниматься своим. Откат — одна ступень «дорожка (сервер)»:
  сервер пишет назад записи компиляторов, как они были до укладки, и снимает свои факелы;
  снос не откатывается. Ступень снимается со стопки только по ответу сервера (молчит 10 с —
  снимается с предупреждением), а пока сервер кладёт какую-то дорожку, откат он откладывает:
  кусок укладки, писавший ту же зону из старых массивов, перезаписал бы его. Кусок, у которого
  за минуту не догрузились объекты, кладётся без сноса и факелов — локация без своего
  `Location` не видна ни сносу, ни проверке «нельзя строить». Изгиб и числа из пакета
  сервер держит сам: изгиб до половины хорды, не-числа — отказ. Игрокам — по-прежнему
  своим клиентом, до лимита админа (максимум 200). Кнопка «Продолжить» — начало нового
  куска в конце прошлого, уложенного целиком или хотя бы частью у сервера; откат и
  отключение её убирают. Там же зоны автоматики стали поднимать объекты клетки только после
  её земли. Проверено в игре 11.09.2026: дорожка 186 м со сглаживанием и сносом легла у
  сервера тремя кусками (91 + 91 + 6 м) за 3 с, два компилятора рельефа он создал сам, снёс
  102 дерева и камня, оставив стоящее в локациях; ошибок ни у клиента, ни у сервера. Куски
  по 91 м, а не по 100: путь — кривая Безье, и точки на ней чуть ближе метра. Не проверены:
  откат, факелы от лица сервера, дорожка через зоны, где никто не бывал. Ход работы —
  строки `Road job` в `BepInEx\LogOutput.log` сервера.
- «Заполнить» → «Стена по краю пола» (`Plugin.PerimeterWall.cs`) с 11.09.2026: игрок стоит на
  полу, мод заливает ячейками 0,5 м весь пол этой высоты под ним (внутренние стены не мешают)
  и ставит стены по внешнему краю, где их ещё нет; дыра в середине — лестничный проём —
  стенами не обносится. Высота 1–4 м своей страницей, по умолчанию 3: полустена
  `wood_wall_half` снизу и `woodwall` сверху, как в «Доме (Модуль)» хозяина; метр в ширину —
  `wood_wall_quarter` стопкой. Панели 2 м ложатся по сетке плит от угла той, на которой
  стоишь, стена — серединой на краю пола, лицом наружу, как в чертежах. Проёма под дверь нет
  — выбор хозяина. Проекция идёт за игроком с пола на пол, с земли остаётся; P закрепляет.
  Игрокам — правило «Стена» (нет/даром/платно, по умолчанию платно), даром — с паузой
  `#wall`. В игре не проверено ничего: ни высота опоры стен (в лог пишется), ни пол, лежащий
  не по сетке.
- «Постройки» → «Настройки» (`Plugin.BuildSettings.cs`) с 11.09.2026: туда ушли
  «Прилипание», «Выравнивать землю», поле дистанции проекции и новый «Разрушать: вкл/выкл» —
  по просьбе хозяина, чтобы страница «Построек» держала только то, что строится. У игрока
  там же прилипание и разрушение, каждое по своему правилу («Прилипание», «Снос»), а
  «Заполнить пол» стал «Заполнить» со страницей пола и стены. «Разрушать» сносит под
  шаблоном, копией, полом и стеной то же, что снос дорожки, но только там, куда встанут
  детали: плоская коробка коллайдеров каждой детали плюс 0,3 м и её высота ±0,5 м; чужие
  обереги у игрока держат. «Отменить постройку» снесённое не вернёт. В игре не проверено.
  С 12.09.2026 там же «Ресурсы: вкл/выкл» — только про постройки самого админа, игроков
  переключатель не касается, у них всё по их правилам. Включено — шаблон, копия, пол, стена,
  забор, мост, факелы вдоль дорожки и заправка при ремонте берутся из его сумки тем же счётом
  `Bill`: оплата целиком до начала, недостроенное возвращается под ноги, «Отменить постройку»
  отдаёт материалы. Выключено — как было, даром. `NoCostCheat` игры по-прежнему освобождает от
  всего. Тогда же закрыто найденное хозяином в игре: у игрока на этой странице было только
  «Прилипание», потому что «Выравнивать землю» показывалось лишь админу, хотя правило
  «Выравнивание» он открыл. Теперь эта кнопка идёт по правилу «Выравнивание» (и в меню, и в
  самой постройке), площадка под постройкой урезается до лимита игрока и говорит об этом в
  углу экрана, а поле дистанции проекции открыто всем — разрешать там нечего.
- Мост у неадмина бесплатный, а разобранная молотом деталь возвращает всё, что стоила,
  — выходит печать дерева. Факелы ровно поэтому платные.
- Дорожка, площадка, выравнивание и мост у неадмина с 10.09.2026 смотрят на чужие
  обереги (варды, `PrivateArea`; `Plugin.Wards.cs`) и отказывают целиком — до записи
  отката и до создания компилятора, а начало дорожки и моста остаётся отмеченным. В игре
  отказ ещё ни разу не срабатывал. Откат на обереги не смотрит и честно смотреть не
  может: он пишет назад зону целиком и стирает всё, что в ней успели сделать другие, —
  в том числе под их оберегами.
- Погода и выбор стороны изгиба дорожки в игре проверены, ветер — только сброс.
  Кнопка «По направлению» и высота в «Ознакомиться» ещё не нажимались.
- Мост строился трижды и стоит; пивот `wood_pole2` оказался по центру, поправлено.
  Не проверялись в игре: марш лестниц до земли, крытая надстройка целиком,
  завершение по ЛКМ, рельеф от лица неадмина.
- Крыша кладётся одной линией конька по осевой, то есть накрывает только настил
  шириной 1. Для широкого моста нужны скаты, которых в конструкции нет.
- Погода и ветер из панели видны только тому, кто нажал: `m_forceEnv` и
  `m_debugWind` локальные. Сделать их общими на сервер — отдельная работа,
  через RPC под `ServerAllows`.
- Из четырёх правок по спавнерам в игре видели только установку: лог показал
  `Ghost preview: 1/1 pieces` и `Placing spawner Spawner_Greydwarf_Elite`. Не
  нажимались: кнопка «Убрать рядом», пропуск выравнивания земли, вынос крыши.
- Автоматизация после починки RPC ни разу не выполнялась. Проверять с сундуком-складом
  у плавильни и у ферментера — до починки первое роняло всю корутину, а второе
  уничтожало содержимое склада.
- Условия самого `CreatureSpawner` в игре не проверялись: не сработает внутри базы
  игрока (`m_spawnInPlayerBase`), может сам себя удалить внутри локации
  (`m_blockSpawnGroups`), требует игрока ближе `m_triggerDistance` (60 м по умолчанию).
  Значения этих полей лежат в данных префабов, из кода их не прочитать — только опытом.

- **«Поставить сундуки»** (19.09.2026, `Plugin.ChestRow.cs`). Кнопка в «Функциях»: пара
  сундуков встаёт **по середине той детали, на которую смотришь**, вплотную друг к другу и
  на её верх. Молотком стена сундуков собирается по одному и ровной не выходит — он ставит
  от точки прицела по своей сетке, а полка под ним лежит по своей, и перекос виден, только
  когда стена уже собрана. Ширину ряда мод **меряет у самого сундука** (коробка коллайдеров
  в его собственных осях, по восьми углам — дочерний объект бывает повёрнут), а не помнит
  числом: сундуки у игры разные, и «вплотную» у каждого своё. Оттуда же берётся низ коробки,
  иначе деталь с началом отсчёта в середине утонула бы в полке наполовину. Угол берётся у
  самой детали и к сетке игры **не округляется**, в отличие от обычной проекции: округлив,
  пару развернуло бы на полке наискось. Дальше как у всякой проекции: ЛКМ ставит, Esc
  убирает, P закрепляет — на том месте, где она стоит, а не у ног, — стрелки двигают
  закреплённую, Q и E поворачивают, Shift+Q/E поднимают: на полке бывает два этажа.
  Землю под собой эта постройка не равняет: сундуки на втором этаже — не повод рыть
  площадку под домом. Вид сундука выбирается кнопкой из тех, что есть в этой сборке игры
  (деталь плюс контейнер; имена сверены по `manifest_extended`, а не по памяти). Игрокам —
  правило «Сундуки» в «Функциях», по умолчанию платно, как пол и забор. 7 тестов на
  раскладку ряда, всего 303. **В игре не проверено ничем**: ни привязка к детали, ни
  поворот, ни оплата, ни то, что два сундука и правда закрывают плиту.

- **Shift значит «и дальше»** (19.09.2026). Одно слово на три места, чтобы стену сундуков
  можно было пройти за один заход, а не за десять походов в панель. **Shift+ЛКМ** у любой
  проекции — построить и остаться с ней в руках (`KeepInHand` в `Plugin.Blueprints.cs`; там
  же возвращается признак бесплатной постройки игрока, который сама постройка гасит, —
  без него второй клик прошёл бы мимо паузы сервера, и это была бы дыра, а не удобство).
  **Shift при открытии сундука** — пометка сортировщика (`MarkChest`) и назначение подачи
  или сбора (`SetChestRole`) остаются в руках, и следующий сундук получит то же самое.
  Читает это одно место — `HoldingShift` в `Plugin.Pin.cs`, рядом с остальными общими
  клавишами. Игра Shift у нас не отнимает: при клике это бег, а «alt» у взаимодействия —
  левый Alt, не он. Отменяется всё одинаково: отпустить Shift на последнем или открыть
  панель — она и так гасит незавершённую пометку. **В игре не проверено.**
- **«Функции» → «Работа с сундуками»** (19.09.2026): «Наполнение», «Сбор», «Зона станций»
  и «Поставить сундуки» съехались на одну страницу — делают они одно дело и нужны обычно
  подряд, а стояли вперемешку с ремонтом и сортировкой. «Назад» с каждой из них ведёт
  теперь туда же.

- **Перепроверка сортировки идёт, пока есть что перекладывать** (19.09.2026,
  `Plugin.Sorting.cs`). Раскладка нигде не хранится и считается заново каждый проход, а
  `TidyBins` каждую секунду переносит из помеченных сундуков то, что по нынешней раскладке
  лежит не там, — на это всегда оставлен хотя бы один ход. Но на базе, где весь вечер
  приезжают повозки, трёх ходов в секунду на это не остаётся, поэтому есть и кнопка
  «Перепроверить» («Функции» → «Сортировка»): помеченные сундуки идут первыми и ходов
  двенадцать в секунду. **Кончается она проходом, которому нечего было переложить**, а не
  по часам: пятнадцать секунд, стоявшие здесь сначала, были числом с потолка и обрывались
  посреди стены сундуков. Предел всё же есть — минута и 600 перекладываний, — иначе
  раскладка, которая почему-то колеблется, гоняла бы сундуки до вечера. Итог говорится
  тремя способами, и третий важен: «переложено N», «всё на своих местах» и «сортировка
  сейчас не идёт» — последнее, когда ни одного прохода не было вовсе, потому что игрок вне
  зоны или в этой зоне разбирает другой. **Пометка сундука запускает перепроверку сама**,
  молча: пометил или снял — раскладка с этой секунды другая, и ждать тут нечего. **В игре
  не проверено.**

Закрыто и в списке больше не нужно: падение на теле больше 1 МБ, `GET /%`,
короткий A2S-пакет, отброшенное отклонение промиса, старт без `JWT_SECRET`,
роль из токена в `requireRole` (11.09.2026: теперь читается из базы, проверено
разжалованным админом с прежней кукой — 403).
